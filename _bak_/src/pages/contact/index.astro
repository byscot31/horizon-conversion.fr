---
import Layout from "@layouts/Layout.astro";
import HubspotCustomForm from "@components/HubspotCustomForm.astro";

const brand = import.meta.env.PUBLIC_BRAND_NAME ?? "Horizon Conversion";
const regionLabel = import.meta.env.PUBLIC_REGION_LABEL ?? "Sud Oise (60) / Nord 95";

const PHONE = import.meta.env.PUBLIC_PHONE ?? "+33660921008";
const PHONE_DISPLAY = import.meta.env.PUBLIC_PHONE_DISPLAY ?? "06 60 92 10 08";
const telHref = `tel:${String(PHONE).replace(/\s+/g, "")}`;

const title = `Contact / Devis gratuit — ${regionLabel} | ${brand}`;
const description = `Demande de devis gratuit : dépannage, installation, travaux. Réponse rapide ${regionLabel}.`;
---

<Layout title={title} description={description} canonical="/contact/">
  <section class="page-title page-title-center dark include-header include-topbar">
    <div class="container">
      <div class="page-title-row mt-5">
        <div class="page-title-content">
          <div class="badge rounded-pill border border-light text-light">Contact</div>
          <h1>Devis gratuit & rappel rapide</h1>
          <p class="lead mb-0">{regionLabel}</p>
        </div>

        <nav aria-label="breadcrumb">
          <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/">Accueil</a></li>
            <li class="breadcrumb-item active" aria-current="page">Contact</li>
          </ol>
        </nav>
      </div>
    </div>
  </section>

  <section id="content">
    <div class="content-wrap pb-0">
      <div class="container">

        <div class="row g-4 align-items-start">
          <div class="col-lg-6">
            <div class="badge rounded-pill badge-default mb-2">Réponse rapide</div>
            <h2 class="display-6 fw-bold mb-3">Expliquez votre besoin, nous vous rappelons</h2>
            <p class="text-muted mb-4">
              Indiquez l’adresse, le niveau d’urgence et le problème. Nous vous proposons un créneau rapidement.
            </p>

            <div class="row g-3">
              <div class="col-md-6">
                <div class="card h-100">
                  <div class="card-body p-4">
                    <h3 class="h6 mb-2">✅ Devis clair</h3>
                    <p class="small text-muted mb-0">Le devis est annoncé avant intervention (hors cas particuliers).</p>
                  </div>
                </div>
              </div>

              <div class="col-md-6">
                <div class="card h-100">
                  <div class="card-body p-4">
                    <h3 class="h6 mb-2">✅ Intervention locale</h3>
                    <p class="small text-muted mb-0">Intervention sur {regionLabel} et alentours.</p>
                  </div>
                </div>
              </div>

              <div class="col-md-6">
                <div class="card h-100">
                  <div class="card-body p-4">
                    <h3 class="h6 mb-2">✅ Conseils</h3>
                    <p class="small text-muted mb-0">Si possible, vous recevez des premiers conseils avant déplacement.</p>
                  </div>
                </div>
              </div>

              <div class="col-md-6">
                <div class="card h-100">
                  <div class="card-body p-4">
                    <h3 class="h6 mb-2">✅ Suivi</h3>
                    <p class="small text-muted mb-0">Nous vous rappelons et planifions l’intervention.</p>
                  </div>
                </div>
              </div>
            </div>

            <div class="d-flex flex-wrap gap-2 mt-4">
              <a class="btn btn-outline-secondary btn-sm" href={telHref}>📞 Appeler {PHONE_DISPLAY}</a>
              <a class="btn btn-outline-primary btn-sm" href="#form">📝 Faire une demande</a>
            </div>
          </div>

          <div class="col-lg-6" id="form">
            <HubspotCustomForm
              title="Demande de devis"
              subtitle="Nous recevons automatiquement les informations utiles (page, ville, service) afin de vous répondre plus vite."
              submitLabel="Envoyer ma demande"
            />
          </div>
        </div>

        <div class="clear line my-6"></div>

        <div class="section mt-0" style="padding: 70px 0;">
          <div class="container">
            <div class="row justify-content-between align-items-center g-4">
              <div class="col-md-7">
                <div class="heading-block border-bottom-0 mb-2">
                  <div class="badge rounded-pill badge-default">Urgence</div>
                  <h2 class="text-transform-none ls-0 mb-0">Si c’est urgent : appelez directement</h2>
                </div>
                <p class="text-muted mb-0">
                  Vous gagnerez du temps : indiquez l’adresse et décrivez brièvement le souci.
                </p>
              </div>
              <div class="col-md-5 text-md-end">
                <a href={telHref} class="button button-rounded button-large m-0">
                  📞 {PHONE_DISPLAY}
                </a>
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>
</Layout>

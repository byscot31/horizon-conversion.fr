---
// src/pages/a-propos.astro
import LayoutSEO from "../layouts/LayoutSEO.astro";
import { NAP } from "../data/nap.ts";

const baseUrl = NAP.url;
const canonical = `${baseUrl}${Astro.url.pathname}`;

const title = "À propos | Horizon Conversion Animaux";
const description =
  "Qui est Horizon Conversion Animaux : webmarketing local pour pros des animaux (60/95). Méthode, zones d’intervention, tracking et approche orientée leads.";

const breadcrumbs = [
  { name: "Accueil", item: `${baseUrl}/` },
  { name: "À propos", item: canonical },
];

const jsonLd = [
  {
    "@context": "https://schema.org",
    "@type": "AboutPage",
    name: title,
    url: canonical,
    description,
    isPartOf: { "@type": "WebSite", name: NAP.name, url: baseUrl },
  },
];
---

<LayoutSEO
  title={title}
  description={description}
  baseUrl={baseUrl}
  canonical={canonical}
  ogType="website"
  ogImage={`${baseUrl}/og/a-propos.jpg`}
  ogImageAlt="À propos – Horizon Conversion Animaux"
  breadcrumbs={breadcrumbs}
  jsonLd={jsonLd}
>

<style>
  .wrap { max-width: 1040px; margin: 0 auto; padding: 28px 16px 72px; }
  .breadcrumbs { font-size: 14px; color: #475569; margin: 4px 0 18px; }
  .breadcrumbs a { color: inherit; text-decoration: none; }
  header.hero {
    border: 1px solid #e2e8f0; border-radius: 18px;
    background: linear-gradient(180deg, #f8fafc, #fff);
    padding: 22px; display: grid; gap: 10px;
  }
  h1 { margin: 0; font-size: 34px; line-height: 1.1; letter-spacing: -0.02em; }
  .sub { margin: 0; color: #334155; max-width: 90ch; line-height: 1.6; }

  .grid { margin-top: 18px; display: grid; grid-template-columns: 1.15fr .85fr; gap: 18px; align-items: start; }
  @media (max-width: 920px){ .grid{ grid-template-columns:1fr; } }

  .card { border: 1px solid #e2e8f0; border-radius: 18px; padding: 18px; background:#fff; box-shadow: 0 6px 20px rgba(15,23,42,.06); }
  h2 { margin: 0 0 10px; font-size: 18px; }
  p { margin: 0; color:#334155; line-height:1.6; }
  ul { margin: 10px 0 0; padding-left: 18px; color:#334155; line-height:1.55; }

  .btnRow { margin-top: 14px; display:grid; grid-template-columns:1fr 1fr; gap:12px; }
  @media (max-width: 720px){ .btnRow{ grid-template-columns:1fr; } }
  .btn { display:inline-flex; align-items:center; justify-content:center; padding:12px 14px; border-radius:14px; border:1px solid #0f172a; background:#0f172a; color:#fff; font-weight:900; text-decoration:none; }
  .btn.secondary { background:#fff; color:#0f172a; }
</style>

<div class="wrap">
  <nav class="breadcrumbs" aria-label="Fil d’Ariane">
    <a href="/">Accueil</a> <span aria-hidden="true">›</span> <span>À propos</span>
  </nav>

  <header class="hero">
    <h1>À propos</h1>
    <p class="sub">
      {NAP.name} accompagne les professionnels des animaux avec une approche “preuve par la donnée” :
      SEO local + Google Maps + Ads, pages dédiées et tracking (appels, formulaires, RDV).
      Zones : <strong>{NAP.areaServed.join(" • ")}</strong>.
    </p>
  </header>

  <section class="grid">
    <div class="card">
      <h2>Pourquoi “Animaux”</h2>
      <p>
        Parce que les métiers animaliers ont des contraintes très spécifiques :
        confiance, urgence, géographie réelle (déplacements), et saisonnalité.
        On structure l’acquisition autour de pages locales et d’intentions fortes.
      </p>

      <h2 style="margin-top:16px;">Comment on travaille</h2>
      <ul>
        <li>Audit & plan priorisé (rapide à exécuter)</li>
        <li>Pages locales “1 intention = 1 page” + FAQ + schémas</li>
        <li>Optimisation Google Business Profile</li>
        <li>Ads cadrées (exclusions, ciblage local, landing)</li>
        <li>Tracking & pilotage (leads, appels, coût/lead)</li>
      </ul>

      <div class="btnRow">
        <a class="btn" href="/contact/">Demander un diagnostic</a>
        <a class="btn secondary" href={`tel:${NAP.phoneE164}`}>Appeler : {NAP.phoneDisplay}</a>
      </div>
    </div>

    <aside class="card">
      <h2>Infos & zone de service</h2>
      <p>
        Zone : <strong>{NAP.areaServed.join(" • ")}</strong><br />
        Email : <a href={`mailto:${NAP.email}`}>{NAP.email}</a><br />
        Téléphone : <a href={`tel:${NAP.phoneE164}`}>{NAP.phoneDisplay}</a>
      </p>

      <h2 style="margin-top:16px;">Liens utiles</h2>
      <ul>
        <li><a href="/preuves/">Preuves</a></li>
        <li><a href="/methode/">Méthode</a></li>
        <li><a href="/prestations/">Prestations</a></li>
        <li><a href="/zones/">Zones</a></li>
      </ul>
    </aside>
  </section>
</div>
---
// src/pages/contact/index.astro

import PageLayout from "../../layouts/PageLayout.astro";
import ContactForm from "../../components/ContactForm.astro";
import site from "../../data/site.json";
import ctas from "../../data/ctas.json";
---

<PageLayout
  title={`Contact — ${site.brand}`}
  description="Audit gratuit 15 min, rappel ou devis. Réponse sous 24h."
>
  <section class="section bg-transparent m-0" style="padding: 80px 0;">
    <div class="container">
      <div class="row justify-content-between align-items-start g-5">
        <div class="col-lg-6">
          <div class="badge rounded-pill badge-default">{site.targetAreaLabel}</div>
          <h1 class="text-transform-none ls-0 mt-3">Contact</h1>

          <p class="lead mb-4">
            Dites-moi ce que vous voulez obtenir. Je vous réponds vite.
          </p>

          <div class="d-flex flex-wrap gap-2 mb-4">
            <a href={ctas.primary.href} class="button button-rounded">{ctas.primary.label}</a>
            <a href={ctas.secondary.href} class="button button-rounded button-light text-dark bg-white border">{ctas.secondary.label}</a>
            <a href={ctas.tertiary.href} class="button button-rounded button-light text-dark bg-white border">{ctas.tertiary.label}</a>
          </div>

          <div class="p-4 rounded-6 bg-white shadow-sm">
            <div class="fw-semibold mb-2">Infos directes</div>
            <div class="text-contrast-600">
              Téléphone : <a href={`tel:${site.phone}`}>{site.phone}</a><br />
              Email : <a href={`mailto:${site.email}`}>{site.email}</a><br />
              Zone : {site.targetAreaLabel}
            </div>
          </div>

          <div class="mt-4">
            <h3 class="text-transform-none ls-0">Ce qu’on fait pendant l’audit</h3>
            <ul>
              <li>On voit d’où viennent (ou non) les demandes.</li>
              <li>On repère 3 priorités concrètes.</li>
              <li>Vous repartez avec un plan simple.</li>
            </ul>
          </div>
        </div>

        <div class="col-lg-5">
          <ContactForm />
        </div>
      </div>
    </div>
  </section>
</PageLayout>
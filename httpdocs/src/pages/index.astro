---
// src/pages/index.astro
import Layout from "../layouts/Layout.astro";
import services from "../data/services.json";
import metiers from "../data/metiers.json";
import villes from "../data/villes.json";

const servicesPremium = services.filter(s => s.premium);
const villesPrincipales = villes.slice(0, 6);
---

<Layout
	title="Expert SEO local Oise & Val-d'Oise"
	description="Horizon Conversion : consultant SEO avec méthode d'agence. J'accompagne artisans, commerces et entreprises à Beauvais, Compiègne, Cergy et dans tout le 60 et 95."
>

	<!-- Hero Section -->
	<section class="bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50 py-20">
		<div class="container-custom">
			<div class="max-w-4xl mx-auto text-center">
				<!-- Badge hybride -->
				<div class="mb-6">
          <span class="inline-flex gap-2">
            <span class="badge-consultant">👤 Consultant indépendant</span>
            <span class="badge-agence">🏢 Méthode d'agence</span>
          </span>
				</div>

				<h1 class="text-5xl md:text-6xl font-bold text-gray-900 mb-6">
					Expert SEO local dans <span class="text-primary-600">l'Oise</span> et le <span class="text-primary-600">Val-d'Oise</span>
				</h1>

				<p class="text-xl text-gray-700 mb-8 max-w-3xl mx-auto leading-relaxed">
					Je suis <strong>consultant indépendant</strong> (vous parlez directement à l'expert)
					mais je travaille avec <strong>les process d'une agence</strong> (méthode, reporting, résultats).
				</p>

				<div class="flex flex-col sm:flex-row gap-4 justify-center">
					<a href="/contact" class="btn-primary text-lg px-8 py-4">
						Discutons de votre projet
					</a>
					<a href="/methodologie" class="btn-secondary text-lg px-8 py-4">
						Découvrir ma méthode
					</a>
				</div>

				<div class="mt-8 flex items-center justify-center gap-4 text-sm text-gray-600">
					<span>📞 06 60 92 10 08</span>
					<span class="w-1 h-1 bg-gray-400 rounded-full"></span>
					<span>📍 Beauvais - Cergy</span>
				</div>
			</div>
		</div>
	</section>

	<!-- Section des personas -->
	<section class="py-20 bg-white">
		<div class="container-custom">
			<h2 class="text-3xl font-bold text-center mb-4">Vous êtes plutôt...</h2>
			<p class="text-center text-gray-600 mb-12 max-w-2xl mx-auto">
				Je ne fais pas de "SEO générique". Chaque métier a ses spécificités, ses clients, ses urgences.
			</p>

			<div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
				<a href="/metiers/artisans" class="group text-center">
					<div class="aspect-square bg-blue-50 rounded-2xl p-6 mb-3 group-hover:bg-blue-100 transition-colors">
						<span class="text-4xl">🔧</span>
					</div>
					<h3 class="font-semibold">Artisans</h3>
					<p class="text-xs text-gray-500">Comme Robert</p>
					<p class="text-xs text-primary-600 mt-1">Urgence : élevée</p>
				</a>

				<a href="/metiers/commerces" class="group text-center">
					<div class="aspect-square bg-green-50 rounded-2xl p-6 mb-3 group-hover:bg-green-100 transition-colors">
						<span class="text-4xl">🍷</span>
					</div>
					<h3 class="font-semibold">Commerces</h3>
					<p class="text-xs text-gray-500">Comme Sophie</p>
					<p class="text-xs text-primary-600 mt-1">Besoin : nouveaux clients</p>
				</a>

				<a href="/metiers/profession-liberale" class="group text-center">
					<div class="aspect-square bg-purple-50 rounded-2xl p-6 mb-3 group-hover:bg-purple-100 transition-colors">
						<span class="text-4xl">⚖️</span>
					</div>
					<h3 class="font-semibold">Prof. libérales</h3>
					<p class="text-xs text-gray-500">Comme Maître Dupont</p>
					<p class="text-xs text-primary-600 mt-1">Recherche : confiance</p>
				</a>

				<a href="/metiers/tpe-pme" class="group text-center">
					<div class="aspect-square bg-amber-50 rounded-2xl p-6 mb-3 group-hover:bg-amber-100 transition-colors">
						<span class="text-4xl">🚛</span>
					</div>
					<h3 class="font-semibold">TPE/PME</h3>
					<p class="text-xs text-gray-500">Comme Franck</p>
					<p class="text-xs text-primary-600 mt-1">Besoin : devis B2B</p>
				</a>

				<a href="/metiers/reseaux-franchise" class="group text-center">
					<div class="aspect-square bg-indigo-50 rounded-2xl p-6 mb-3 group-hover:bg-indigo-100 transition-colors">
						<span class="text-4xl">🥖</span>
					</div>
					<h3 class="font-semibold">Franchises</h3>
					<p class="text-xs text-gray-500">Comme Caroline</p>
					<p class="text-xs text-primary-600 mt-1">Multi-sites</p>
				</a>

				<a href="/metiers/tourisme" class="group text-center">
					<div class="aspect-square bg-rose-50 rounded-2xl p-6 mb-3 group-hover:bg-rose-100 transition-colors">
						<span class="text-4xl">🏨</span>
					</div>
					<h3 class="font-semibold">Tourisme</h3>
					<p class="text-xs text-gray-500">Comme Virginie</p>
					<p class="text-xs text-primary-600 mt-1">Objectif : direct</p>
				</a>
			</div>
		</div>
	</section>

	<!-- Services Section -->
	<section class="py-20">
		<div class="container-custom">
			<h2 class="text-3xl font-bold text-center mb-4">Mes services</h2>
			<p class="text-gray-600 text-center mb-12 max-w-2xl mx-auto">
				Des solutions adaptées à chaque métier, chaque territoire.
			</p>

			<div class="grid grid-cols-1 md:grid-cols-3 gap-8">
				{servicesPremium.map(service => (
					<div class="card p-8 text-center hover:shadow-xl transition-all">
						<div class="w-16 h-16 bg-primary-50 rounded-full flex items-center justify-center mx-auto mb-4">
							<svg class="w-8 h-8 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
								<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
							</svg>
						</div>
						<h3 class="text-xl font-bold mb-2">{service.nom}</h3>
						<p class="text-gray-600 mb-4">{service.description}</p>
						<p class="text-sm font-semibold text-primary-600 mb-4">{service.benefice}</p>
						<a href={`/services/${service.id}`} class="text-primary-600 font-medium hover:underline inline-flex items-center gap-1">
							En savoir plus
							<span>→</span>
						</a>
					</div>
				))}
			</div>

			<div class="text-center mt-12">
				<a href="/services" class="btn-secondary">
					Voir tous les services
				</a>
			</div>
		</div>
	</section>

	<!-- Positionnement hybride expliqué -->
	<section class="bg-gray-50 py-20">
		<div class="container-custom">
			<div class="grid md:grid-cols-2 gap-12">
				<div class="bg-white p-8 rounded-xl shadow-md">
					<div class="badge-consultant inline-block mb-4">👤 Consultant</div>
					<h3 class="text-2xl font-bold mb-4">L'expertise directe</h3>
					<ul class="space-y-3 text-gray-700">
						<li class="flex items-start gap-2">✓ Vous travaillez directement avec moi, pas de commercial</li>
						<li class="flex items-start gap-2">✓ 100% de votre budget va à l'expertise</li>
						<li class="flex items-start gap-2">✓ Relation durable, je suis là sur le long terme</li>
						<li class="flex items-start gap-2">✓ Connaissance terrain du 60 et 95</li>
					</ul>
				</div>

				<div class="bg-white p-8 rounded-xl shadow-md">
					<div class="badge-agence inline-block mb-4">🏢 Méthode d'agence</div>
					<h3 class="text-2xl font-bold mb-4">La fiabilité structurée</h3>
					<ul class="space-y-3 text-gray-700">
						<li class="flex items-start gap-2">✓ Process éprouvés sur des dizaines de clients</li>
						<li class="flex items-start gap-2">✓ Reporting professionnels (Data Studio)</li>
						<li class="flex items-start gap-2">✓ Outils experts (SEMrush, Ahrefs)</li>
						<li class="flex items-start gap-2">✓ Équipe de partenaires si besoin</li>
					</ul>
				</div>
			</div>

			<p class="text-center text-xl font-semibold text-primary-600 mt-8">
				Le meilleur des deux mondes, pour vous.
			</p>
		</div>
	</section>

	<!-- Métiers Section -->
	<section class="py-20">
		<div class="container-custom">
			<h2 class="text-3xl font-bold text-center mb-4">J'accompagne</h2>
			<p class="text-gray-600 text-center mb-12 max-w-2xl mx-auto">
				Chaque métier a ses spécificités. J'adapte mon approche à vos besoins.
			</p>

			<div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
				{metiers.map(metier => (
					<a href={`/metiers/${metier.id}`} class="card p-4 text-center hover:scale-105 transition-transform group">
						<div class="font-semibold text-gray-900 group-hover:text-primary-600">{metier.nomCourt}</div>
						<div class="text-xs text-gray-500 mt-1">{metier.description.split(',')[0]}</div>
					</a>
				))}
			</div>
		</div>
	</section>

	<!-- Villes Section -->
	<section class="bg-gray-50 py-20">
		<div class="container-custom">
			<h2 class="text-3xl font-bold text-center mb-4">Mes villes d'intervention</h2>
			<p class="text-gray-600 text-center mb-12 max-w-2xl mx-auto">
				Je me déplace dans tout l'Oise (60) et le Val-d'Oise (95).
			</p>

			<div class="grid grid-cols-2 md:grid-cols-4 gap-4">
				{villesPrincipales.map(ville => (
					<a href={`/villes/${ville.id}`} class="card p-4 text-center hover:border-primary-300 transition-colors">
						<div class="font-semibold">{ville.nom}</div>
						<div class="text-sm text-gray-500">{ville.codePostal}</div>
						<div class="text-xs text-primary-600 mt-1">{ville.zone === "60" ? "Oise" : "Val-d'Oise"}</div>
					</a>
				))}
			</div>

			<div class="text-center mt-8">
				<a href="/villes" class="text-primary-600 font-medium hover:underline inline-flex items-center gap-1">
					Voir les 16 villes et leurs alentours
					<span>→</span>
				</a>
			</div>
		</div>
	</section>

	<!-- Section des hubs -->
	<section class="py-12 bg-gray-50">
		<div class="container-custom">
			<h2 class="text-2xl font-bold text-center mb-8">Découvrez nos ressources</h2>
			<div class="grid grid-cols-2 md:grid-cols-4 gap-4">
				<a href="/services" class="bg-white p-4 rounded-xl text-center hover:shadow-md">
					<span class="text-3xl mb-2 block">🛠️</span>
					<span class="font-semibold">Tous nos services</span>
				</a>
				<a href="/metiers" class="bg-white p-4 rounded-xl text-center hover:shadow-md">
					<span class="text-3xl mb-2 block">👥</span>
					<span class="font-semibold">Tous les métiers</span>
				</a>
				<a href="/villes" class="bg-white p-4 rounded-xl text-center hover:shadow-md">
					<span class="text-3xl mb-2 block">📍</span>
					<span class="font-semibold">Toutes nos villes</span>
				</a>
				<a href="/etudes-de-cas" class="bg-white p-4 rounded-xl text-center hover:shadow-md">
					<span class="text-3xl mb-2 block">📊</span>
					<span class="font-semibold">Études de cas</span>
				</a>
			</div>
		</div>
	</section>

	<!-- CTA Final -->
	<section class="py-16">
		<div class="container-custom">
			<div class="bg-primary-600 text-white rounded-2xl p-12 text-center">
				<h2 class="text-3xl font-bold mb-4">Prêt à passer à l'action ?</h2>
				<p class="text-xl mb-8 opacity-90 max-w-2xl mx-auto">
					Que vous soyez artisan, commerce ou entreprise dans l'Oise ou le Val-d'Oise, je vous propose un audit gratuit de votre visibilité en ligne.
				</p>
				<a href="/contact" class="bg-white text-primary-600 hover:bg-gray-100 px-8 py-4 rounded-full font-semibold text-lg inline-block transition-colors">
					Demander mon audit gratuit
				</a>
			</div>
		</div>
	</section>

</Layout>
---
// src/pages/metiers/[metier].astro
import Layout from "../../layouts/Layout.astro";
import services from "../../data/services.json";
import metiers from "../../data/metiers.json";
import villes from "../../data/villes.json";
import MiniDevis from "../../components/MiniDevis.astro";
import casClients from "../../data/cas-clients.json";

export async function getStaticPaths() {
  return metiers.map(metier => ({
    params: { metier: metier.id },
    props: { metier }
  }));
}

const { metier } = Astro.props;

// Services recommandés pour ce métier
const servicesConseilles = services.filter(s =>
  metier.id === 'artisans' ? ['seo-local', 'gmb', 'creation-site'].includes(s.id) :
    metier.id === 'commerces' ? ['google-ads', 'gmb', 'seo-local'].includes(s.id) :
      metier.id === 'profession-liberale' ? ['cro', 'seo-local', 'formation'].includes(s.id) :
        metier.id === 'tpe-pme' ? ['creation-site', 'seo-local', 'google-ads'].includes(s.id) :
          metier.id === 'reseaux-franchise' ? ['gmb', 'google-ads', 'cro'].includes(s.id) :
            metier.id === 'tourisme' ? ['seo-local', 'google-ads', 'cro'].includes(s.id) :
              []
);

// Villes où ce métier est le plus présent
const villesMetier = villes.filter(v =>
  metier.id === 'artisans' ? ['meru', 'beauvais', 'creil', 'chambly', 'noyon'].includes(v.id) :
    metier.id === 'commerces' ? ['cergy', 'argenteuil', 'compiegne', 'pontoise'].includes(v.id) :
      metier.id === 'profession-liberale' ? ['chantilly', 'montmorency', 'senlis', 'beauvais'].includes(v.id) :
        metier.id === 'tpe-pme' ? ['creil', 'cergy', 'beauvais', 'compiegne'].includes(v.id) :
          metier.id === 'reseaux-franchise' ? ['cergy', 'argenteuil', 'compiegne', 'pontoise'].includes(v.id) :
            metier.id === 'tourisme' ? ['chantilly', 'compiegne', 'auvers-sur-oise', 'senlis'].includes(v.id) :
              []
).slice(0, 8);

// Couleur selon le métier
const bgColor = {
  artisans: "bg-blue-50",
  commerces: "bg-green-50",
  "profession-liberale": "bg-purple-50",
  "tpe-pme": "bg-amber-50",
  "reseaux-franchise": "bg-indigo-50",
  tourisme: "bg-rose-50"
}[metier.id] || "bg-gray-50";

// Mapping des cas clients par métier
const casParMetier = {
  artisans: [
    { nom: "Robert", entreprise: "Plomberie Martin", ville: "Méru", resultat: "+650% d'appels", etude: "plombier-meru", service: "seo-local" },
    { nom: "Jean-Claude", entreprise: "Lefebvre Électricité", ville: "Beauvais", resultat: "+400% de visibilité", etude: "electricien-beauvais", service: "seo-local" },
    { nom: "Michel", entreprise: "Menuiserie Dubois", ville: "Chambly", resultat: "+500% de devis", etude: "menuisier-chambly", service: "creation-site" }
  ],
  commerces: [
    { nom: "Sophie", entreprise: "Cave des 3 Rois", ville: "Cergy", resultat: "+30% de clients", etude: "caviste-cergy", service: "google-ads" },
    { nom: "Isabelle", entreprise: "Coiffure Isabelle", ville: "Argenteuil", resultat: "+35% de clients", etude: "coiffeuse-argenteuil", service: "gmb" },
    { nom: "Céline", entreprise: "Fleuriste Céline", ville: "Cergy", resultat: "+40% de CA", etude: "fleuriste-cergy", service: "seo-local" }
  ],
  "profession-liberale": [
    { nom: "Maître Dupont", entreprise: "Cabinet Dupont", ville: "Montmorency", resultat: "+200% de rendez-vous", etude: "avocat-montmorency", service: "cro" },
    { nom: "Dr. Martin", entreprise: "Médecine générale", ville: "Senlis", resultat: "+180% de rendez-vous", etude: "medecin-senlis", service: "cro" },
    { nom: "Claire", entreprise: "Atelier Architecture", ville: "Montmorency", resultat: "+200% de devis", etude: "architecte-montmorency", service: "creation-site" }
  ],
  "tpe-pme": [
    { nom: "Franck", entreprise: "Transports Moreau", ville: "Compiègne", resultat: "+150% de devis", etude: "transport-compiegne", service: "creation-site" },
    { nom: "Philippe", entreprise: "SARL BTP 60", ville: "Creil", resultat: "+300% de CA", etude: "macon-creil", service: "seo-local" },
    { nom: "Sylvie", entreprise: "Beauvais Nettoyage", ville: "Beauvais", resultat: "+250% contrats pros", etude: "nettoyage-beauvais", service: "seo-local" }
  ],
  "reseaux-franchise": [
    { nom: "Caroline", entreprise: "Boulangeries du Pain", ville: "Cergy", resultat: "+450 avis", etude: "franchise-cergy", service: "gmb" }
  ],
  tourisme: [
    { nom: "Virginie", entreprise: "Hôtel du Parc", ville: "Chantilly", resultat: "+80% réservations", etude: "hotel-chantilly", service: "seo-local" },
    { nom: "Pierre", entreprise: "Restaurant", ville: "Compiègne", resultat: "+200 couverts", etude: "restaurateur-compiegne", service: "gmb" },
    { nom: "Sophie", entreprise: "Gîte Les Rêves d'Ô", ville: "Senlis", resultat: "+80% réservations", etude: "gite-senlis", service: "seo-local" }
  ]
};

const casMetier = casParMetier[metier.id] || [];
const autresMetiers = metiers.filter(m => m.id !== metier.id).slice(0, 5);
---

<Layout
    title={metier.nom}
    description={`Accompagnement SEO pour les ${metier.nomCourt} dans l'Oise et le Val-d'Oise. ${metier.description}`}
>

  <!-- Hero -->
  <section class={`${bgColor} py-20`}>
    <div class="container-custom">
      <div class="max-w-4xl mx-auto">
        <nav class="text-sm text-gray-600 mb-8">
          <a href="/metiers" class="hover:text-primary-600">Métiers</a> ›
          <span class="text-gray-900">{metier.nom}</span>
        </nav>

        <h1 class="text-5xl font-bold text-gray-900 mb-4">{metier.nom}</h1>
        <p class="text-xl text-gray-700 mb-6">{metier.description}</p>

        <div class="flex flex-wrap gap-3">
          <span class="bg-white px-4 py-2 rounded-full text-sm shadow-sm">🎯 {metier.besoin}</span>
          <span class="bg-white px-4 py-2 rounded-full text-sm shadow-sm">⚡ Urgence : {metier.urgence}</span>
        </div>

        <p class="mt-8 text-lg italic text-gray-700">"{metier.message?.replace('{ville}', 'votre ville')}"</p>

        <div class="mt-8">
          <a href="/contact" class="btn-primary text-lg px-8 py-4">
            Demander mon audit gratuit
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- NOUVEAU BLOC : Les 3 défis des [métier] -->
  <section class="py-20 bg-white">
    <div class="container-custom">
      <h2 class="text-3xl font-bold text-center mb-4">Les 3 défis des {metier.nomCourt}</h2>
      <p class="text-center text-gray-600 mb-12 max-w-2xl mx-auto">
        Des problématiques spécifiques à votre métier.
      </p>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto">

        <!-- DÉFI 1 -->
        <div class="bg-gray-50 p-6 rounded-xl hover:shadow-md transition-all">
          <div class="text-3xl mb-3">1️⃣</div>
          <h3 class="text-xl font-bold mb-2">
            {metier.id === 'artisans' && "Urgences et dépannage"}
            {metier.id === 'commerces' && "Attirer en boutique"}
            {metier.id === 'profession-liberale' && "Instaurer la confiance"}
            {metier.id === 'tpe-pme' && "Générer des leads B2B"}
            {metier.id === 'reseaux-franchise' && "Cohérence multi-sites"}
            {metier.id === 'tourisme' && "Dépendance aux plateformes"}
          </h3>
          <p class="text-gray-600">
            {metier.id === 'artisans' && "Les clients cherchent un dépanneur immédiatement disponible, pas dans 3 jours. Si vous n'apparaissez pas dans les premiers résultats, ils appellent vos concurrents."}
            {metier.id === 'commerces' && "Faire venir de nouveaux clients sans exploser le budget pub, et les fidéliser pour qu'ils reviennent. Le bouche-à-oreille ne suffit plus."}
            {metier.id === 'profession-liberale' && "Les patients/clients comparent longuement avant de choisir un professionnel. Avis Google, site de confiance, tout compte."}
            {metier.id === 'tpe-pme' && "Toucher des décideurs B2B et pas seulement des particuliers. Les entreprises cherchent des prestataires sérieux en ligne."}
            {metier.id === 'reseaux-franchise' && "Chaque point de vente fait sa propre communication, pas de cohérence, et des fiches Google souvent négligées."}
            {metier.id === 'tourisme' && "Perdre 15-20% de marge sur les plateformes comme Booking ou Airbnb, et ne pas connaître ses clients."}
          </p>
        </div>

        <!-- DÉFI 2 -->
        <div class="bg-gray-50 p-6 rounded-xl hover:shadow-md transition-all">
          <div class="text-3xl mb-3">2️⃣</div>
          <h3 class="text-xl font-bold mb-2">
            {metier.id === 'artisans' && "Clientèle fidèle mais vieillissante"}
            {metier.id === 'commerces' && "Concurrence des grandes surfaces / e-commerce"}
            {metier.id === 'profession-liberale' && "Pas de visibilité pour les nouveaux arrivants"}
            {metier.id === 'tpe-pme' && "Site vieillissant qui ne convertit pas"}
            {metier.id === 'reseaux-franchise' && "Difficulté à gérer les avis clients"}
            {metier.id === 'tourisme' && "Saisonnalité et remplissage hors vacances"}
          </h3>
          <p class="text-gray-600">
            {metier.id === 'artisans' && "Vous vivez du bouche-à-oreille, mais les nouveaux habitants ne vous trouvent pas. Votre clientèle ne se renouvelle pas assez."}
            {metier.id === 'commerces' && "Les clients comparent les prix sur internet avant d'acheter. Il faut leur donner une raison de pousser votre porte."}
            {metier.id === 'profession-liberale' && "Les nouveaux arrivants dans la région cherchent un médecin, un avocat... mais ne vous trouvent pas sur Google."}
            {metier.id === 'tpe-pme' && "Votre site a 10 ans, il ne donne pas confiance et ne génère pas de devis. Vous perdez des parts de marché."}
            {metier.id === 'reseaux-franchise' && "Les avis Google sont mal gérés, certains points de vente ont des notes basses qui impactent tout le réseau."}
            {metier.id === 'tourisme' && "Comment remplir votre établissement en basse saison ? Comment attirer une clientèle locale ?"}
          </p>
        </div>

        <!-- DÉFI 3 -->
        <div class="bg-gray-50 p-6 rounded-xl hover:shadow-md transition-all">
          <div class="text-3xl mb-3">3️⃣</div>
          <h3 class="text-xl font-bold mb-2">
            {metier.id === 'artisans' && "Se démarquer de la concurrence"}
            {metier.id === 'commerces' && "Fidéliser la clientèle existante"}
            {metier.id === 'profession-liberale' && "Gérer sa e-réputation"}
            {metier.id === 'tpe-pme' && "Se faire connaître localement"}
            {metier.id === 'reseaux-franchise' && "Centraliser sans perdre le local"}
            {metier.id === 'tourisme' && "Fidéliser et faire revenir les clients"}
          </h3>
          <p class="text-gray-600">
            {metier.id === 'artisans' && "Vos concurrents sont mieux référencés, ils captent les appels d'urgence avant vous. Il faut inverser la tendance."}
            {metier.id === 'commerces' && "Les clients passent, mais ne reviennent pas. Comment créer une relation durable et les fidéliser ?"}
            {metier.id === 'profession-liberale' && "Un mauvais avis peut vous coûter cher. Comment gérer sa réputation en ligne et obtenir des avis positifs ?"}
            {metier.id === 'tpe-pme' && "Vous êtes peu connu en dehors de votre réseau. Comment être visible pour les entreprises de la zone ?"}
            {metier.id === 'reseaux-franchise' && "Comment garder une cohérence de marque tout en permettant à chaque point de vente d'avoir une présence locale ?"}
            {metier.id === 'tourisme' && "Les clients partent sans laisser d'avis et sans réserver directement pour leur prochain séjour. Comment les faire revenir ?"}
          </p>
        </div>
      </div>

      <!-- POUR LES MÉTIERS AVEC PEU DE DÉFIS (ex: franchises) -->
      {metier.id === 'reseaux-franchise' && (
          <p class="text-center text-sm text-gray-500 mt-4">
            * Ces défis sont adaptés aux réseaux de toutes tailles, de 2 à 50 points de vente.
          </p>
      )}
    </div>
  </section>

  <!-- SECTION 1 : Enjeux spécifiques -->
  <section class="py-20">
    <div class="container-custom">
      <div class="grid md:grid-cols-2 gap-12">
        <div>
          <h2 class="text-3xl font-bold mb-6">Les enjeux des {metier.nomCourt}</h2>

          <ul class="space-y-4">
            <li class="flex items-start gap-3">
              <span class="text-primary-600 text-xl mt-0.5">✓</span>
              <div>
                <span class="font-semibold">Visibilité locale</span>
                <p class="text-gray-600">Être trouvé quand les clients cherchent près de chez eux.</p>
              </div>
            </li>

            <li class="flex items-start gap-3">
              <span class="text-primary-600 text-xl mt-0.5">✓</span>
              <div>
                <span class="font-semibold">Confiance et crédibilité</span>
                <p class="text-gray-600">Avis Google, présence dans les annuaires, site professionnel.</p>
              </div>
            </li>

            <li class="flex items-start gap-3">
              <span class="text-primary-600 text-xl mt-0.5">✓</span>
              <div>
                <span class="font-semibold">Différenciation concurrentielle</span>
                <p class="text-gray-600">Se démarquer des autres professionnels du même secteur.</p>
              </div>
            </li>
          </ul>
        </div>

        <div class={`${bgColor} p-8 rounded-xl`}>
          <h2 class="text-2xl font-bold mb-4">Mon approche pour les {metier.nomCourt}</h2>

          <p class="text-gray-700 mb-4">
            Je ne propose pas une solution standard. J'adapte ma stratégie à votre activité spécifique.
          </p>

          <div class="bg-white p-4 rounded-lg">
            <div class="font-semibold text-primary-600">Exemple pour {metier.nomCourt} :</div>
            <p class="text-sm text-gray-600 mt-2">
              {metier.id === 'artisans' && "Création de pages par service (plomberie, électricité) et par ville, optimisation de la fiche Google, suivi des avis."}
              {metier.id === 'commerces' && "Campagnes Google Ads géolocalisées, photos professionnelles, posts réguliers sur la fiche Google."}
              {metier.id === 'profession-liberale' && "Contenu d'autorité, optimisation du taux de rendez-vous, gestion de l'e-réputation."}
              {metier.id === 'tpe-pme' && "Site vitrine professionnel, SEO B2B, campagnes LinkedIn si pertinent."}
              {metier.id === 'reseaux-franchise' && "Gestion centralisée des fiches Google, formation des gérants, audits réguliers."}
              {metier.id === 'tourisme' && "Photos de qualité, contenu sur les activités locales, avis Google soignés."}
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- MINI-DEVIS ESTIMATEUR (NOUVEAU) -->
  <section class="py-12 bg-white">
    <div class="container-custom">
      <MiniDevis />
    </div>
  </section>

  <!-- SECTION 2 : Services recommandés (LIENS CORRIGÉS) -->
  <section class="bg-gray-50 py-20">
    <div class="container-custom">
      <h2 class="text-3xl font-bold text-center mb-12">Services clés pour les {metier.nomCourt}</h2>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {servicesConseilles.map(service => (
            <a href={`/services/${service.id}/${metier.id}`} class="card p-6 text-center hover:shadow-xl group">
              <h3 class="text-xl font-bold mb-2 group-hover:text-primary-600">{service.nom}</h3>
              <p class="text-gray-600 text-sm mb-3">{service.description}</p>
              <span class="text-primary-600 font-medium">Découvrir →</span>
            </a>
        ))}
      </div>
    </div>
  </section>

  <!-- SECTION 3 : Résultats concrets (DYNAMIQUE) -->
  {(() => {
    // Récupérer les cas pour ce métier depuis le JSON centralisé
  const casPourCeMetier = casClients[metier.id] || [];

    if (casPourCeMetier.length === 0) return null;

    return (
        <section class="py-20">
          <div class="container-custom">
            <h2 class="text-3xl font-bold text-center mb-4">Des résultats pour des {metier.nomCourt}</h2>
            <p class="text-center text-gray-600 mb-12">Découvrez comment nous avons aidé des professionnels comme vous.</p>

            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
              {casPourCeMetier.slice(0, 3).map(cas => (
                  <div class="bg-white p-6 rounded-xl shadow-md hover:shadow-lg transition-all">
                    <div class="flex justify-between items-start mb-3">
                      <span class="badge-consultant text-xs">{cas.serviceNom || 'SEO'}</span>
                      <span class="text-sm text-gray-500">{cas.ville}</span>
                    </div>
                    <p class="font-semibold text-lg mb-1">{cas.nom}</p>
                    <p class="text-sm text-gray-600 mb-3">{cas.entreprise}</p>
                    <p class="text-2xl font-bold text-green-600 mb-3">{cas.resultat}</p>
                    <a href={`/etudes-de-cas/${cas.etude}`} class="text-primary-600 text-sm hover:underline">
                      Voir l'étude complète →
                    </a>
                  </div>
              ))}
            </div>

            <div class="text-center mt-8">
              <a href={`/etudes-de-cas?metier=${metier.id}`} class="text-primary-600 hover:underline font-medium">
                Voir toutes les études de cas {metier.nomCourt} →
              </a>
            </div>
          </div>
        </section>
    );
  })()}

  <!-- SECTION 4 : Villes clés avec liens profonds -->
  <section class="bg-gray-50 py-20">
    <div class="container-custom">
      <h2 class="text-3xl font-bold text-center mb-4">Où j'accompagne des {metier.nomCourt}</h2>
      <p class="text-center text-gray-600 mb-12">Dans tout l'Oise et le Val-d'Oise, avec une présence forte dans ces villes.</p>

      <div class="grid grid-cols-2 md:grid-cols-4 gap-4 max-w-4xl mx-auto">
        {villesMetier.map(ville => (
            <div class="bg-white p-4 rounded-xl shadow-sm hover:shadow-md transition-all">
              <div class="font-semibold text-center mb-2">{ville.nom}</div>
              <div class="space-y-2 text-sm">
                {servicesConseilles.slice(0, 3).map(service => (
                    <a href={`/services/${service.id}/${metier.id}/${ville.id}`}
                       class="block text-primary-600 hover:underline text-center">
                      {service.nomCourt} →
                    </a>
                ))}
              </div>
            </div>
        ))}
      </div>

      <div class="text-center mt-8">
        <a href="/villes" class="text-primary-600 hover:underline">Voir toutes les villes →</a>
      </div>
    </div>
  </section>

  <!-- SECTION 5 : Témoignages clients (DYNAMIQUE) -->
  {(() => {
    const casPourCeMetier = casClients[metier.id] || [];
    const premiersCas = casPourCeMetier.slice(0, 2);

    if (premiersCas.length === 0) return null;

    return (
        <section class="py-20 bg-primary-50">
          <div class="container-custom">
            <h2 class="text-3xl font-bold text-center mb-4">Ils nous ont fait confiance</h2>
            <p class="text-center text-gray-600 mb-12">Découvrez ce que disent d'autres {metier.nomCourt}.</p>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-4xl mx-auto">
              {premiersCas.map(cas => (
                  <div class="bg-white p-8 rounded-2xl shadow-md">
                    <div class="flex items-start gap-4">
                      <div class="w-16 h-16 bg-primary-100 rounded-full flex items-center justify-center flex-shrink-0 text-3xl">
                        {cas.icone || '👤'}
                      </div>
                      <div>
                        <div class="text-yellow-400 text-lg mb-2">{"★".repeat(cas.note || 5)}</div>
                        <p class="text-lg italic text-gray-700 mb-4">"{cas.citation}"</p>
                        <p class="font-semibold">{cas.nom}, {cas.entreprise}</p>
                        <a href={`/etudes-de-cas/${cas.etude}`} class="inline-block mt-3 text-primary-600 text-sm hover:underline">
                          Voir son histoire →
                        </a>
                      </div>
                    </div>
                  </div>
              ))}
            </div>

            <div class="text-center mt-8">
              <a href="/etudes-de-cas" class="text-primary-600 hover:underline font-medium">
                Voir toutes les études de cas →
              </a>
            </div>
          </div>
        </section>
    );
  })()}

  <!-- SECTION 6 : Ressources métier -->
  <section class="bg-gray-50 py-20">
    <div class="container-custom">
      <h2 class="text-3xl font-bold text-center mb-4">Ressources pour les {metier.nomCourt}</h2>
      <p class="text-center text-gray-600 mb-12">Guides et articles pour approfondir.</p>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
        <a href="/ressources/guides/seo-local-artisans-2025" class="bg-white p-6 rounded-xl border border-gray-100 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">📘</div>
          <h3 class="font-bold mb-2">Guide SEO local pour {metier.nomCourt}</h3>
          <p class="text-sm text-gray-600 mb-3">Toutes les clés pour être visible dans votre ville.</p>
          <span class="text-primary-600 text-sm">Télécharger →</span>
        </a>

        <a href="/ressources/blog/avis-google-importance" class="bg-white p-6 rounded-xl border border-gray-100 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">📝</div>
          <h3 class="font-bold mb-2">Pourquoi les avis sont cruciaux</h3>
          <p class="text-sm text-gray-600 mb-3">Comment obtenir plus d'avis et mieux convertir.</p>
          <span class="text-primary-600 text-sm">Lire l'article →</span>
        </a>

        <a href="/methodologie" class="bg-white p-6 rounded-xl border border-gray-100 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">🔍</div>
          <h3 class="font-bold mb-2">Ma méthode</h3>
          <p class="text-sm text-gray-600 mb-3">Découvrez comment je travaille en 4 phases.</p>
          <span class="text-primary-600 text-sm">Découvrir →</span>
        </a>
      </div>
    </div>
  </section>

  <!-- SECTION 7 : FAQ métier -->
  <section class="py-20">
    <div class="container-custom max-w-3xl">
      <h2 class="text-3xl font-bold text-center mb-4">Questions fréquentes</h2>
      <p class="text-center text-gray-600 mb-12">Sur le SEO pour les {metier.nomCourt}.</p>

      <div class="space-y-4">
        <details class="bg-gray-50 p-6 rounded-lg">
          <summary class="font-semibold cursor-pointer">Combien de temps pour voir des résultats pour un {metier.nomCourt} ?</summary>
          <p class="mt-3 text-gray-600">Les premiers résultats apparaissent généralement entre 1 et 3 mois selon votre secteur et votre zone. Pour les {metier.nomCourt}, on voit souvent des améliorations rapides sur Google My Business.</p>
        </details>

        <details class="bg-gray-50 p-6 rounded-lg">
          <summary class="font-semibold cursor-pointer">Quel budget pour un {metier.nomCourt} ?</summary>
          <p class="mt-3 text-gray-600">Je propose des devis personnalisés après un audit gratuit. Pour les {metier.nomCourt}, comptez entre 300€ et 800€ par mois selon vos objectifs.</p>
        </details>

        <details class="bg-gray-50 p-6 rounded-lg">
          <summary class="font-semibold cursor-pointer">Quel service est le plus adapté pour un {metier.nomCourt} ?</summary>
          <p class="mt-3 text-gray-600">
            {metier.id === 'artisans' && "Le SEO local et la fiche Google My Business sont essentiels. La création d'un site avec portfolio est également recommandée."}
            {metier.id === 'commerces' && "Google Ads pour des résultats rapides, complété par une fiche Google optimisée et du SEO local."}
            {metier.id === 'profession-liberale' && "Le CRO pour convertir les visiteurs en rendez-vous, associé à du contenu d'expertise."}
            {metier.id === 'tpe-pme' && "Un site professionnel associé à du SEO B2B et des campagnes ciblées."}
            {metier.id === 'reseaux-franchise' && "La gestion centralisée des fiches Google est la priorité, avec des campagnes locales."}
            {metier.id === 'tourisme' && "Le SEO local et un contenu de qualité sur les activités environnantes."}
          </p>
        </details>
      </div>

      <div class="text-center mt-8">
        <a href="/ressources/faq" class="text-primary-600 hover:underline">
          Voir toutes les FAQ →
        </a>
      </div>
    </div>
  </section>

  <!-- SECTION 8 : Autres métiers -->
  <section class="bg-gray-50 py-16">
    <div class="container-custom">
      <h3 class="text-lg font-semibold text-center mb-6">Découvrez aussi nos accompagnements pour :</h3>
      <div class="flex flex-wrap justify-center gap-3">
        {autresMetiers.map(autreMetier => (
            <a
                href={`/metiers/${autreMetier.id}`}
                class="bg-white px-4 py-2 rounded-full text-sm hover:bg-primary-100 hover:text-primary-600 transition-colors"
            >
              {autreMetier.nomCourt}
            </a>
        ))}
        <a href="/metiers" class="bg-primary-50 text-primary-600 px-4 py-2 rounded-full text-sm hover:bg-primary-100 transition-colors">
          Tous les métiers
        </a>
      </div>
    </div>
  </section>

  <!-- SECTION 9 : Pourquoi moi -->
  <section class="py-20">
    <div class="container-custom">
      <div class="grid md:grid-cols-2 gap-12">
        <div>
          <h2 class="text-3xl font-bold mb-6">Pourquoi travailler avec moi ?</h2>
          <ul class="space-y-4">
            <li class="flex items-start gap-3">
              <span class="text-primary-600 text-xl">✓</span>
              <div>
                <span class="font-semibold">Je connais votre métier</span>
                <p class="text-gray-600">J'accompagne des {metier.nomCourt} depuis plusieurs années, je connais vos enjeux.</p>
              </div>
            </li>
            <li class="flex items-start gap-3">
              <span class="text-primary-600 text-xl">✓</span>
              <div>
                <span class="font-semibold">Stratégie sur-mesure</span>
                <p class="text-gray-600">Pas de solution standard, chaque plan est adapté à votre activité et votre zone.</p>
              </div>
            </li>
            <li class="flex items-start gap-3">
              <span class="text-primary-600 text-xl">✓</span>
              <div>
                <span class="font-semibold">Présent localement</span>
                <p class="text-gray-600">Basé dans l'Oise, je me déplace facilement pour vous rencontrer.</p>
              </div>
            </li>
          </ul>
        </div>

        <div class="bg-gray-50 p-8 rounded-xl">
          <h2 class="text-2xl font-bold mb-4">Prêt à discuter ?</h2>
          <p class="text-gray-700 mb-6">
            Je propose un audit gratuit pour analyser votre situation et voir comment je peux aider votre {metier.nomCourt}.
          </p>
          <a href="/contact" class="btn-primary inline-block w-full text-center">
            Demander mon audit gratuit
          </a>
          <p class="text-sm text-gray-500 mt-4 text-center">
            📞 06 60 92 10 08 • contact@horizon-conversion.fr
          </p>
        </div>
      </div>
    </div>
  </section>

</Layout>
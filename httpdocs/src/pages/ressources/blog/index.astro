---
import Layout from "../../../layouts/Layout.astro";

// Données des articles (à externaliser dans un fichier JSON plus tard)
const articles = [
  {
    slug: "seo-local-artisans-2025",
    titre: "SEO local pour artisans : le guide complet 2025",
    excerpt: "Comment être trouvé quand un client cherche un plombier, électricien ou menuisier dans votre ville.",
    date: "2025-02-15",
    categorie: "Guide",
    image: "/images/ressources/seo-artisans.jpg",
    auteur: "Julien",
    tempsLecture: 8,
    vues: 1245
  },
  {
    slug: "google-ads-petits-budgets",
    titre: "Google Ads avec petit budget : 5 stratégies qui marchent",
    excerpt: "Comment faire des annonces rentables avec moins de 300€ par mois pour un commerce local.",
    date: "2025-02-10",
    categorie: "Stratégie",
    image: "/images/ressources/ads-budget.jpg",
    auteur: "Julien",
    tempsLecture: 6,
    vues: 892
  },
  {
    slug: "avis-google-importance",
    titre: "Pourquoi les avis Google sont devenus plus importants que votre site",
    excerpt: "L'impact des avis sur le classement local et comment obtenir plus d'avis naturellement.",
    date: "2025-02-05",
    categorie: "E-réputation",
    image: "/images/ressources/avis-google.jpg",
    auteur: "Julien",
    tempsLecture: 5,
    vues: 1567
  },
  {
    slug: "cro-pour-professions-liberales",
    titre: "CRO pour professions libérales : transformer les visites en rendez-vous",
    excerpt: "Techniques spécifiques pour avocats, médecins et architectes qui veulent plus de clients.",
    date: "2025-01-28",
    categorie: "Guide",
    image: "/images/ressources/cro-pro.jpg",
    auteur: "Julien",
    tempsLecture: 7,
    vues: 734
  },
  {
    slug: "referencement-local-google-maps",
    titre: "Google Maps : le guide pour être dans le pack local",
    excerpt: "Les 12 actions concrètes pour apparaître dans les 3 premiers résultats avec carte.",
    date: "2025-01-20",
    categorie: "SEO local",
    image: "/images/ressources/google-maps.jpg",
    auteur: "Julien",
    tempsLecture: 9,
    vues: 2103
  },
  {
    slug: "prix-seo-local-2025",
    titre: "Combien coûte vraiment le SEO local en 2025 ?",
    excerpt: "Analyse des prix du marché et ce que vous devez vraiment payer pour des résultats.",
    date: "2025-01-12",
    categorie: "Conseils",
    image: "/images/ressources/prix-seo.jpg",
    auteur: "Julien",
    tempsLecture: 6,
    vues: 3456
  }
];

// Pagination
const url = new URL(Astro.request.url);
const page = parseInt(url.searchParams.get('page') || '1');
const articlesParPage = 6;
const totalPages = Math.ceil(articles.length / articlesParPage);
const start = (page - 1) * articlesParPage;
const end = start + articlesParPage;
const articlesPage = articles.slice(start, end);
---

<Layout
    title="Blog SEO et marketing local | Horizon Conversion"
    description="Articles et conseils gratuits pour améliorer votre visibilité locale dans l'Oise et le Val-d'Oise."
>

  <!-- Hero -->
  <section class="bg-gradient-to-br from-primary-50 to-indigo-50 py-16">
    <div class="container-custom text-center">
      <h1 class="text-5xl font-bold text-gray-900 mb-4">
        Le <span class="text-primary-600">blog</span>
      </h1>
      <p class="text-xl text-gray-700 mb-8 max-w-3xl mx-auto">
        Conseils, guides et astuces pour comprendre le SEO local sans jargon technique.
      </p>

      <!-- Barre de recherche (simulée) -->
      <div class="max-w-md mx-auto">
        <div class="relative">
          <input
              type="search"
              placeholder="Rechercher un article..."
              class="w-full px-6 py-4 rounded-full border border-gray-200 focus:outline-none focus:border-primary-500"
          >
          <button class="absolute right-2 top-2 bg-primary-600 text-white px-6 py-2 rounded-full hover:bg-primary-700 transition-colors">
            🔍
          </button>
        </div>
      </div>
    </div>
  </section>

  <!-- Filtres par catégorie -->
  <section class="py-8 border-b">
    <div class="container-custom">
      <div class="flex flex-wrap gap-3 justify-center">
        <a href="/ressources/blog" class="px-4 py-2 bg-primary-600 text-white rounded-full text-sm">Tous</a>
        <a href="/ressources/blog?categorie=guide" class="px-4 py-2 bg-gray-100 hover:bg-gray-200 rounded-full text-sm">Guides</a>
        <a href="/ressources/blog?categorie=strategie" class="px-4 py-2 bg-gray-100 hover:bg-gray-200 rounded-full text-sm">Stratégie</a>
        <a href="/ressources/blog?categorie=seo-local" class="px-4 py-2 bg-gray-100 hover:bg-gray-200 rounded-full text-sm">SEO local</a>
        <a href="/ressources/blog?categorie=conseils" class="px-4 py-2 bg-gray-100 hover:bg-gray-200 rounded-full text-sm">Conseils</a>
        <a href="/ressources/blog?categorie=e-reputation" class="px-4 py-2 bg-gray-100 hover:bg-gray-200 rounded-full text-sm">E-réputation</a>
      </div>
    </div>
  </section>

  <!-- Grille des articles -->
  <section class="py-16">
    <div class="container-custom">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {articlesPage.map(article => (
            <a href={`/ressources/blog/${article.slug}`} class="card group hover:shadow-xl transition-all overflow-hidden">
              <div class="aspect-video bg-gray-100 relative overflow-hidden">
                <img
                    src={article.image}
                    alt={article.titre}
                    class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                    onerror="this.src='https://placehold.co/600x400/2563eb/white?text=Article'"
                >
                <div class="absolute top-2 left-2">
                <span class="bg-white/90 backdrop-blur-sm px-3 py-1 rounded-full text-xs font-medium">
                  {article.categorie}
                </span>
                </div>
              </div>

              <div class="p-6">
                <div class="flex items-center gap-3 text-xs text-gray-500 mb-3">
                  <span>{new Date(article.date).toLocaleDateString('fr-FR')}</span>
                  <span>•</span>
                  <span>{article.tempsLecture} min</span>
                  <span>•</span>
                  <span>👁️ {article.vues}</span>
                </div>

                <h2 class="text-xl font-bold mb-2 group-hover:text-primary-600 transition-colors line-clamp-2">
                  {article.titre}
                </h2>

                <p class="text-gray-600 text-sm mb-4 line-clamp-2">
                  {article.excerpt}
                </p>

                <div class="flex items-center justify-between">
                  <span class="text-sm text-gray-500">Par {article.auteur}</span>
                  <span class="text-primary-600 group-hover:translate-x-2 transition-transform">Lire →</span>
                </div>
              </div>
            </a>
        ))}
      </div>

      <!-- Pagination -->
      {totalPages > 1 && (
          <div class="flex justify-center mt-12 gap-2">
            {page > 1 && (
                <a href={`/ressources/blog?page=${page - 1}`} class="px-4 py-2 border rounded-lg hover:bg-gray-50">
                  ← Précédent
                </a>
            )}

            {Array.from({ length: totalPages }, (_, i) => i + 1).map(num => (
                <a
                    href={`/ressources/blog?page=${num}`}
                    class={`px-4 py-2 border rounded-lg ${
                      num === page ? 'bg-primary-600 text-white border-primary-600' : 'hover:bg-gray-50'
                    }`}
                >
                  {num}
                </a>
            ))}

            {page < totalPages && (
                <a href={`/ressources/blog?page=${page + 1}`} class="px-4 py-2 border rounded-lg hover:bg-gray-50">
                  Suivant →
                </a>
            )}
          </div>
      )}
    </div>
  </section>

  <!-- Newsletter -->
  <section class="bg-primary-50 py-16">
    <div class="container-custom text-center max-w-2xl">
      <h2 class="text-3xl font-bold mb-4">Ne manquez aucun article</h2>
      <p class="text-gray-600 mb-8">
        Recevez mes nouveaux articles directement par email (une fois par mois maximum).
      </p>

      <form class="flex flex-col sm:flex-row gap-4">
        <input
            type="email"
            placeholder="Votre email"
            class="flex-1 px-4 py-3 rounded-lg border border-gray-200 focus:outline-none focus:border-primary-500"
            required
        >
        <button class="btn-primary whitespace-nowrap">
          S'inscrire
        </button>
      </form>
      <p class="text-xs text-gray-500 mt-4">Pas de spam, désinscription à tout moment.</p>
    </div>
  </section>

</Layout>
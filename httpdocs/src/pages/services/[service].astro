---
// src/pages/services/[service].astro
import Layout from "../../layouts/Layout.astro";
import services from "../../data/services.json";
import metiers from "../../data/metiers.json";
import villes from "../../data/villes.json";

export async function getStaticPaths() {
  return services.map(service => ({
    params: { service: service.id },
    props: { service }
  }));
}

const { service } = Astro.props;

// Métiers recommandés pour ce service
const metiersConseilles = metiers.filter(m =>
  service.id === 'seo-local' ? ['artisans', 'commerces', 'profession-liberale'].includes(m.id) :
    service.id === 'google-ads' ? ['commerces', 'tpe-pme', 'reseaux-franchise'].includes(m.id) :
      service.id === 'cro' ? ['profession-liberale', 'tpe-pme', 'reseaux-franchise'].includes(m.id) :
        service.id === 'creation-site' ? ['artisans', 'commerces', 'tpe-pme'].includes(m.id) :
          service.id === 'gmb' ? ['artisans', 'commerces', 'reseaux-franchise'].includes(m.id) :
            service.id === 'formation' ? ['artisans', 'commerces', 'profession-liberale'].includes(m.id) :
              []
).slice(0, 4);

// Villes principales pour les exemples
const villesExemples = villes.slice(0, 6);
---

<Layout
    title={`${service.nom} | Horizon Conversion`}
    description={service.description}
>

  <!-- Hero (inchangé) -->
  <section class="bg-gradient-to-br from-primary-50 to-indigo-50 py-20">
    <div class="container-custom">
      <div class="max-w-4xl mx-auto">
        <nav class="text-sm text-gray-600 mb-8">
          <a href="/services" class="hover:text-primary-600">Services</a> ›
          <span class="text-gray-900">{service.nom}</span>
        </nav>

        <h1 class="text-5xl font-bold text-gray-900 mb-6">{service.nom}</h1>
        <p class="text-xl text-gray-700 mb-8 max-w-3xl">{service.description}</p>

        <div class="flex flex-wrap gap-4 items-center">
          <span class="bg-white px-4 py-2 rounded-full text-sm shadow-sm">🎯 {service.benefice}</span>
          <span class="bg-primary-100 text-primary-700 px-4 py-2 rounded-full text-sm">
            {service.premium ? '⭐ Service premium' : '🔧 Service complémentaire'}
          </span>
        </div>
      </div>
    </div>
  </section>

  <!-- Bénéfices détaillés (inchangé) -->
  <section class="py-20">
    <div class="container-custom">
      <div class="grid md:grid-cols-2 gap-16">
        <div>
          <h2 class="text-3xl font-bold mb-6">Pourquoi choisir ce service ?</h2>
          <div class="space-y-6">
            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-primary-100 rounded-full flex items-center justify-center flex-shrink-0">
                <span class="text-primary-600 text-xl">✓</span>
              </div>
              <div>
                <h3 class="font-semibold text-lg mb-1">Expertise locale</h3>
                <p class="text-gray-600">Je connais parfaitement le tissu économique de l'Oise et du Val-d'Oise pour adapter ma stratégie.</p>
              </div>
            </div>
            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-primary-100 rounded-full flex items-center justify-center flex-shrink-0">
                <span class="text-primary-600 text-xl">⚡</span>
              </div>
              <div>
                <h3 class="font-semibold text-lg mb-1">Résultats mesurables</h3>
                <p class="text-gray-600">Chaque action est suivie et reportée. Vous savez exactement ce que produit votre investissement.</p>
              </div>
            </div>
            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-primary-100 rounded-full flex items-center justify-center flex-shrink-0">
                <span class="text-primary-600 text-xl">🤝</span>
              </div>
              <div>
                <h3 class="font-semibold text-lg mb-1">Accompagnement direct</h3>
                <p class="text-gray-600">Vous travaillez avec moi, pas avec un commercial ou un chef de projet. Interlocuteur unique.</p>
              </div>
            </div>
          </div>
        </div>
        <div class="bg-gray-50 p-8 rounded-xl">
          <h2 class="text-2xl font-bold mb-6">Comment je le mets en œuvre</h2>
          <div class="space-y-4">
            <div class="bg-white p-4 rounded-lg">
              <div class="font-semibold text-primary-600 mb-1">1. Audit</div>
              <p class="text-sm text-gray-600">Analyse de votre situation actuelle, de vos concurrents, de vos objectifs.</p>
            </div>
            <div class="bg-white p-4 rounded-lg">
              <div class="font-semibold text-primary-600 mb-1">2. Stratégie</div>
              <p class="text-sm text-gray-600">Plan d'action détaillé, adapté à votre métier et votre zone.</p>
            </div>
            <div class="bg-white p-4 rounded-lg">
              <div class="font-semibold text-primary-600 mb-1">3. Exécution</div>
              <p class="text-sm text-gray-600">Mise en place des actions, avec suivi bi-mensuel.</p>
            </div>
            <div class="bg-white p-4 rounded-lg">
              <div class="font-semibold text-primary-600 mb-1">4. Reporting</div>
              <p class="text-sm text-gray-600">Tableaux de bord personnalisés, analyse des résultats.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Pour quels métiers → liens vers /services/service/metier -->
  <section class="bg-gray-50 py-20">
    <div class="container-custom">
      <h2 class="text-3xl font-bold text-center mb-12">Pour quels métiers ?</h2>

      <div class="grid grid-cols-2 md:grid-cols-4 gap-4 max-w-3xl mx-auto">
        {metiersConseilles.map(metier => (
            <a href={`/services/${service.id}/${metier.id}`} class="bg-white p-4 rounded-xl text-center hover:shadow-md transition-all group">
              <div class="font-semibold group-hover:text-primary-600">{metier.nomCourt}</div>
              <div class="text-xs text-gray-500 mt-1">{metier.description}</div>
            </a>
        ))}
      </div>

      <div class="text-center mt-8">
        <a href="/metiers" class="text-primary-600 hover:underline">Voir tous les métiers →</a>
      </div>
    </div>
  </section>

  <!-- Exemples dans les villes → liens vers /services/service/ville -->
  <section class="py-20">
    <div class="container-custom">
      <h2 class="text-3xl font-bold text-center mb-4">Disponible dans toutes nos villes</h2>
      <p class="text-center text-gray-600 mb-12">Je travaille dans tout l'Oise et le Val-d'Oise.</p>

      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
        {villesExemples.map(ville => (
            <a href={`/services/${service.id}/${ville.id}`} class="card p-4 text-center hover:border-primary-300">
              <div class="font-semibold">{ville.nom}</div>
              <div class="text-xs text-gray-500">{ville.codePostal}</div>
            </a>
        ))}
      </div>

      <div class="text-center mt-8">
        <a href="/villes" class="text-primary-600 hover:underline">Voir toutes les villes →</a>
      </div>
    </div>
  </section>

  <!-- Cas client exemple (reste inchangé) -->
  <section class="bg-primary-50 py-16">
    <div class="container-custom">
      <h2 class="text-3xl font-bold text-center mb-12">Un résultat concret</h2>

      <div class="max-w-3xl mx-auto bg-white p-8 rounded-2xl shadow-md">
        <div class="flex justify-between items-start mb-4">
          <span class="badge-consultant">Artisan</span>
          <span class="text-gray-500">Méru (60)</span>
        </div>

        <blockquote class="text-xl italic text-gray-700 mb-6">
          "{service.id === 'seo-local' && "Depuis qu'on a commencé, je reçois des appels de clients que je n'avais jamais eus avant. Mon téléphone n'arrête pas de sonner !"}
          {service.id === 'google-ads' && "Pendant les fêtes, j'ai eu 30% de clients en plus qui disaient 'je vous ai vu sur Google'."}
          {service.id === 'cro' && "Je reçois deux fois plus de demandes de rendez-vous sans changer mes annonces."}
          {service.id === 'creation-site' && "Mon site était vieux, je n'avais pas de leads. Maintenant je reçois des demandes de devis toutes les semaines."}
          {service.id === 'gmb' && "Mes clients me trouvent sur Google Maps et prennent rendez-vous en ligne."}
          {service.id === 'formation' && "Maintenant je gère mon SEO tout seul et je comprends ce que je fais."}"
        </blockquote>

        <p class="font-semibold">Robert Martin</p>
        <p class="text-sm text-gray-600">Plomberie Martin</p>

        <div class="mt-4 text-green-700 font-bold text-xl">
          {service.id === 'seo-local' && "+650% d'appels"}
          {service.id === 'google-ads' && "+30% de clients en boutique"}
          {service.id === 'cro' && "+200% de rendez-vous"}
          {service.id === 'creation-site' && "+400% de trafic"}
          {service.id === 'gmb' && "+450 avis en 6 mois"}
          {service.id === 'formation' && "Autonome en 2 mois"}
        </div>

        <a href="/etudes-de-cas" class="inline-block mt-6 text-primary-600 hover:underline">
          Voir l'étude de cas complète →
        </a>
      </div>
    </div>
  </section>

  <!-- FAQ rapide (inchangé) -->
  <section class="py-20">
    <div class="container-custom max-w-3xl">
      <h2 class="text-3xl font-bold text-center mb-12">Questions fréquentes</h2>

      <div class="space-y-4">
        <details class="bg-gray-50 p-4 rounded-lg">
          <summary class="font-semibold cursor-pointer">Combien de temps avant d'avoir des résultats ?</summary>
          <p class="mt-2 text-gray-600">Les premiers résultats apparaissent généralement entre 1 et 3 mois selon le service et la concurrence locale.</p>
        </details>

        <details class="bg-gray-50 p-4 rounded-lg">
          <summary class="font-semibold cursor-pointer">Quel est l'investissement mensuel ?</summary>
          <p class="mt-2 text-gray-600">Tout dépend de vos objectifs et de votre zone. Je propose des devis personnalisés après un audit gratuit.</p>
        </details>

        <details class="bg-gray-50 p-4 rounded-lg">
          <summary class="font-semibold cursor-pointer">Est-ce que ça marche pour mon métier ?</summary>
          <p class="mt-2 text-gray-600">J'accompagne tous types de métiers. Chaque stratégie est adaptée à votre activité spécifique.</p>
        </details>
      </div>
    </div>
  </section>

  <!-- CTA (inchangé) -->
  <section class="bg-primary-600 text-white py-16">
    <div class="container-custom text-center">
      <h2 class="text-3xl font-bold mb-4">Prêt à discuter de votre projet ?</h2>
      <p class="text-xl mb-8 opacity-90 max-w-2xl mx-auto">
        Parlons de vos objectifs et voyons comment ce service peut vous aider.
      </p>
      <a href="/contact" class="bg-white text-primary-600 hover:bg-gray-100 px-8 py-4 rounded-full font-semibold text-lg inline-block transition-colors">
        Demander un audit gratuit
      </a>
    </div>
  </section>

</Layout>
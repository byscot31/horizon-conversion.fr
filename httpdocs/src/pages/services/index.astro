---
import Layout from "../../layouts/Layout.astro";
import services from "../../data/services.json";
import ScrollAnimation from "../../components/ScrollAnimation.astro";

const servicesPremium = services.filter(s => s.premium);
const servicesComplementaires = services.filter(s => !s.premium);
---

<Layout
    title="Services SEO et marketing digital - Oise et Val-d'Oise | Horizon Conversion"
    description="Découvrez mes services de référencement local, Google Ads, CRO, création de sites et formation pour artisans, commerces et entreprises dans le 60 et 95."
>

  <!-- Hero -->
  <section class="bg-gradient-to-br from-primary-50 to-indigo-50 py-20">
    <div class="container-custom text-center">
      <h1 class="text-5xl font-bold text-gray-900 mb-6">Mes <span class="text-primary-600">services</span></h1>
      <p class="text-xl text-gray-700 mb-8 max-w-3xl mx-auto">
        Des solutions adaptées à chaque métier, chaque territoire. Que vous soyez artisan, commerce ou entreprise dans l'Oise et le Val-d'Oise.
      </p>

      <!-- Stats rapides -->
      <div class="flex justify-center gap-8">
        <div>
          <div class="text-3xl font-bold text-primary-600">6</div>
          <div class="text-sm text-gray-600">services</div>
        </div>
        <div>
          <div class="text-3xl font-bold text-primary-600">3</div>
          <div class="text-sm text-gray-600">services premium</div>
        </div>
        <div>
          <div class="text-3xl font-bold text-primary-600">576</div>
          <div class="text-sm text-gray-600">pages combinées</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Services Premium -->
  <section class="py-20">
    <div class="container-custom">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold mb-4">Services <span class="text-primary-600">premium</span></h2>
        <p class="text-gray-600 max-w-2xl mx-auto">Le cœur de mon expertise : des services complets pour des résultats mesurables.</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {servicesPremium.map(service => (
            <a href={`/services/${service.id}`} class="card group hover:shadow-xl transition-all overflow-hidden">
              <div class="h-48 bg-gradient-to-br from-primary-100 to-primary-50 p-8 flex items-center justify-center">
                <div class="w-24 h-24 bg-white rounded-full flex items-center justify-center shadow-lg">
                  <svg class="w-12 h-12 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    {service.id === 'seo-local' && (
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                    )}
                    {service.id === 'google-ads' && (
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 3.055A9.001 9.001 0 1020.945 13H11V3.055z" />
                    )}
                    {service.id === 'cro' && (
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
                    )}
                  </svg>
                </div>
              </div>

              <div class="p-6">
                <h3 class="text-xl font-bold mb-2 group-hover:text-primary-600 transition-colors">{service.nom}</h3>
                <p class="text-gray-600 text-sm mb-4">{service.description}</p>
                <div class="flex items-center justify-between">
                  <span class="text-sm font-semibold text-primary-600">{service.benefice}</span>
                  <span class="text-primary-600 group-hover:translate-x-2 transition-transform">→</span>
                </div>
              </div>
            </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Services complémentaires -->
  <section class="bg-gray-50 py-20">
    <div class="container-custom">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold mb-4">Services <span class="text-primary-600">complémentaires</span></h2>
        <p class="text-gray-600 max-w-2xl mx-auto">Pour aller plus loin et vous accompagner sur tous les aspects de votre présence en ligne.</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {servicesComplementaires.map(service => (
            <a href={`/services/${service.id}`} class="bg-white p-6 rounded-xl shadow-sm hover:shadow-md transition-all group">
              <h3 class="text-lg font-bold mb-2 group-hover:text-primary-600">{service.nom}</h3>
              <p class="text-gray-600 text-sm mb-3">{service.description}</p>
              <p class="text-xs font-semibold text-primary-600 mb-3">{service.benefice}</p>
              <span class="text-sm text-primary-600 group-hover:translate-x-2 transition-transform inline-block">En savoir plus →</span>
            </a>
        ))}
      </div>
    </div>
  </section>

  <!-- Comment je travaille -->
  <section class="py-20">
    <div class="container-custom">
      <div class="grid md:grid-cols-2 gap-12 items-center">
        <div>
          <h2 class="text-3xl font-bold mb-6">Une approche <span class="text-primary-600">hybride</span></h2>
          <p class="text-lg text-gray-700 mb-6">
            Je combine le meilleur des deux mondes : l'agilité et l'expertise directe d'un consultant avec les process et les outils d'une agence.
          </p>

          <div class="space-y-4">
            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-primary-100 rounded-full flex items-center justify-center flex-shrink-0">
                <span class="text-primary-600 text-xl">👤</span>
              </div>
              <div>
                <h3 class="font-semibold text-lg">Consultant indépendant</h3>
                <p class="text-gray-600">Vous travaillez directement avec moi, pas de commercial, pas de chef de projet.</p>
              </div>
            </div>

            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-primary-100 rounded-full flex items-center justify-center flex-shrink-0">
                <span class="text-primary-600 text-xl">🏢</span>
              </div>
              <div>
                <h3 class="font-semibold text-lg">Méthode d'agence</h3>
                <p class="text-gray-600">Des process structurés, des reporting professionnels, des outils experts.</p>
              </div>
            </div>

            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-primary-100 rounded-full flex items-center justify-center flex-shrink-0">
                <span class="text-primary-600 text-xl">📍</span>
              </div>
              <div>
                <h3 class="font-semibold text-lg">Expertise locale</h3>
                <p class="text-gray-600">Basé dans l'Oise, je connais parfaitement les spécificités du territoire.</p>
              </div>
            </div>
          </div>

          <a href="/methodologie" class="btn-primary inline-block mt-8">
            Découvrir ma méthode
          </a>
        </div>

        <div class="bg-primary-600 text-white p-8 rounded-2xl">
          <h3 class="text-2xl font-bold mb-4">Ce que je ne fais pas</h3>
          <ul class="space-y-3">
            <li class="flex items-start gap-2">✕ Des "packs SEO" standard à 99€</li>
            <li class="flex items-start gap-2">✕ Des backlinks achetés en masse</li>
            <li class="flex items-start gap-2">✕ Des promesses de #1 en 15 jours</li>
            <li class="flex items-start gap-2">✕ Des contrats d'engagement abusifs</li>
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- Exemples de réalisations -->
  <section class="bg-gray-50 py-20">
    <div class="container-custom">
      <h2 class="text-3xl font-bold text-center mb-4">Ils m'ont fait confiance</h2>
      <p class="text-center text-gray-600 mb-12">Quelques résultats concrets obtenus pour mes clients.</p>

      <div class="grid md:grid-cols-3 gap-6">
        <div class="bg-white p-6 rounded-xl">
          <div class="badge-consultant text-xs mb-3">SEO local</div>
          <p class="text-2xl font-bold text-primary-600 mb-2">+650%</p>
          <p class="text-gray-600 text-sm">d'appels pour un plombier à Méru</p>
          <a href="/etudes-de-cas/plombier-meru" class="text-primary-600 text-sm hover:underline mt-3 inline-block">Voir l'étude →</a>
        </div>

        <div class="bg-white p-6 rounded-xl">
          <div class="badge-agence text-xs mb-3">Google Ads</div>
          <p class="text-2xl font-bold text-primary-600 mb-2">+30%</p>
          <p class="text-gray-600 text-sm">de clients en boutique pour une caviste à Cergy</p>
          <a href="/etudes-de-cas/caviste-cergy" class="text-primary-600 text-sm hover:underline mt-3 inline-block">Voir l'étude →</a>
        </div>

        <div class="bg-white p-6 rounded-xl">
          <div class="badge-premium text-xs mb-3">CRO</div>
          <p class="text-2xl font-bold text-primary-600 mb-2">+200%</p>
          <p class="text-gray-600 text-sm">de rendez-vous pour un avocat à Montmorency</p>
          <a href="/etudes-de-cas/avocat-montmorency" class="text-primary-600 text-sm hover:underline mt-3 inline-block">Voir l'étude →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="bg-primary-600 text-white py-16">
    <div class="container-custom text-center">
      <h2 class="text-3xl font-bold mb-4">Prêt à discuter de votre projet ?</h2>
      <p class="text-xl mb-8 opacity-90 max-w-2xl mx-auto">
        Je propose un audit gratuit pour analyser votre situation et voir quel service peut vous aider.
      </p>
      <a href="/contact" class="bg-white text-primary-600 hover:bg-gray-100 px-8 py-4 rounded-full font-semibold text-lg inline-block transition-colors">
        Demander mon audit gratuit
      </a>
    </div>
  </section>

</Layout>
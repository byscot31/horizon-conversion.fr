---
// src/pages/services/[service]/[metier]/[ville].astro
import Layout from "../../../../layouts/Layout.astro";
import services from "../../../../data/services.json";
import metiers from "../../../../data/metiers.json";
import villes from "../../../../data/villes.json";
import intentions from "../../../../data/intentions.json";
import personasParVille from "../../../../data/personas-par-ville.json";
import casClients from "../../../../data/cas-clients.json";
import IntentionAnimation from "../../../../components/IntentionAnimation.astro";

export async function getStaticPaths() {
  const paths = [];
  for (const service of services) {
    for (const metier of metiers) {
      for (const ville of villes) {
        paths.push({
          params: {
            service: service.id,
            metier: metier.id,
            ville: ville.id
          },
          props: { service, metier, ville }
        });
      }
    }
  }
  return paths;
}

const { service, metier, ville } = Astro.props;

// Données d'intention (transactionnelle pour les pages reines)
const intentionData = intentions[metier.id]?.intentions?.transactionnelle || {
  description: `Vous cherchez un expert en ${service.nomCourt} pour votre activité de ${metier.nomCourt} à ${ville.nom} ?`,
  cta: "Obtenir un audit gratuit",
  urgence: "haute"
};

// Récupérer le persona spécifique à cette ville et ce métier
const personaLocal = personasParVille[ville.id]?.[metier.id];

// Fallback sur le persona générique du métier
const personaGenerique = intentions[metier.id]?.persona;

// Déterminer quel persona afficher
const persona = personaLocal || personaGenerique || {
  nom: "nos clients",
  citation: "Je veux être plus visible dans ma ville.",
  histoire: "Comme beaucoup de professionnels, vous cherchez à attirer plus de clients locaux."
};

// Filtrer les cas clients pour ce métier
const casPourCeMetier = casClients[metier.id] || [];

// Priorité : cas dans la même ville, sinon cas du métier
const casMemeVille = casPourCeMetier.filter(c => c.villeId === ville.id);
const casAAfficher = casMemeVille.length >= 3
  ? casMemeVille.slice(0, 3)
  : [...casMemeVille, ...casPourCeMetier.filter(c => c.villeId !== ville.id)].slice(0, 3);

// Si pas assez de cas, utiliser des cas génériques
const TEMOIGNAGES_PAR_DEFAUT = {
  artisans: [
    { nom: "Robert", entreprise: "Plomberie Martin", ville: "Méru", resultat: "+650% d'appels", etude: "plombier-meru", citation: "Depuis qu'on a commencé, je reçois des appels de clients que je n'avais jamais eus avant.", icone: "👤" },
    { nom: "Jean-Claude", entreprise: "Lefebvre Électricité", ville: "Beauvais", resultat: "+400% de visibilité", etude: "electricien-beauvais", citation: "Aujourd'hui, je suis dans le top 3 sur les mots-clés importants.", icone: "🔧" },
    { nom: "Michel", entreprise: "Menuiserie Dubois", ville: "Chambly", resultat: "+500% de devis", etude: "menuisier-chambly", citation: "Mon téléphone sonne constamment.", icone: "🔨" }
  ],
  commerces: [
    { nom: "Sophie", entreprise: "Cave des 3 Rois", ville: "Cergy", resultat: "+30% de clients", etude: "caviste-cergy", citation: "Pendant les fêtes, +30% de clients.", icone: "🍷" },
    { nom: "Isabelle", entreprise: "Coiffure Isabelle", ville: "Argenteuil", resultat: "+35% de clients", etude: "coiffeuse-argenteuil", citation: "Les clients me trouvent sur Google Maps.", icone: "💇" },
    { nom: "Céline", entreprise: "Fleuriste Céline", ville: "Cergy", resultat: "+40% de CA", etude: "fleuriste-cergy", citation: "+40% de chiffre le week-end.", icone: "💐" }
  ],
  "profession-liberale": [
    { nom: "Maître Dupont", entreprise: "Cabinet Dupont", ville: "Montmorency", resultat: "+200% de rendez-vous", etude: "avocat-montmorency", citation: "Je reçois deux fois plus de rendez-vous.", icone: "⚖️" },
    { nom: "Dr. Martin", entreprise: "Médecine générale", ville: "Senlis", resultat: "+180% de rendez-vous", etude: "medecin-senlis", citation: "Mon taux de conversion a doublé.", icone: "👨‍⚕️" },
    { nom: "Claire", entreprise: "Atelier Architecture", ville: "Montmorency", resultat: "+200% de devis", etude: "architecte-montmorency", citation: "Je n'ai plus besoin de publicité.", icone: "📐" }
  ],
  "tpe-pme": [
    { nom: "Franck", entreprise: "Transports Moreau", ville: "Compiègne", resultat: "+150% de devis", etude: "transport-compiegne", citation: "Des devis toutes les semaines.", icone: "🚛" },
    { nom: "Philippe", entreprise: "SARL BTP 60", ville: "Creil", resultat: "+300% de CA", etude: "macon-creil", citation: "Notre CA a augmenté de 40%.", icone: "🏗️" },
    { nom: "Sylvie", entreprise: "Beauvais Nettoyage", ville: "Beauvais", resultat: "+250% contrats pros", etude: "nettoyage-beauvais", citation: "3 contrats majeurs décrochés.", icone: "🧹" }
  ],
  "reseaux-franchise": [
    { nom: "Caroline", entreprise: "Boulangeries du Pain", ville: "Cergy", resultat: "+450 avis", etude: "franchise-cergy", citation: "Gestion centralisée qui nous fait gagner un temps précieux.", icone: "🥖" }
  ],
  tourisme: [
    { nom: "Virginie", entreprise: "Hôtel du Parc", ville: "Chantilly", resultat: "+80% réservations", etude: "hotel-chantilly", citation: "Je ne dépends plus de Booking.", icone: "🏨" },
    { nom: "Pierre", entreprise: "Restaurant", ville: "Compiègne", resultat: "+200 couverts", etude: "restaurateur-compiegne", citation: "30% de réservations directes en plus.", icone: "🍽️" },
    { nom: "Sophie", entreprise: "Gîte Les Rêves d'Ô", ville: "Senlis", resultat: "+80% réservations", etude: "gite-senlis", citation: "Booking ne représente plus que 30% de mon CA.", icone: "🏡" }
  ]
};

const temoignagesAAfficher = casAAfficher.length >= 3
  ? casAAfficher
  : (TEMOIGNAGES_PAR_DEFAUT[metier.id] || []).slice(0, 3);

// Déterminer le texte à afficher selon la situation
const affichagePersona = {
  // Si on a un persona spécifique à la ville, on l'utilise
  avecNom: personaLocal ? `Comme ${persona.nom}, ${metier.nomCourt} à ${ville.nom}...` : null,

  // Sinon, on utilise une formule générique
  sansNom: `Comme d'autres ${metier.nomCourt} à ${ville.nom}...`,

  // Texte à utiliser
  texte: personaLocal
    ? `Comme ${persona.nom}, ${metier.nomCourt} à ${ville.nom}...`
    : `Comme d'autres ${metier.nomCourt} à ${ville.nom}...`
};

// Autres villes pour ce même service+métier
const autresVilles = villes.filter(v => v.id !== ville.id).slice(0, 8);

// Autres services pour ce même métier dans cette ville
const autresServices = services.filter(s => s.id !== service.id).slice(0, 4);

// Titre et description
const titre = `${service.nom} pour ${metier.nomCourt} à ${ville.nom}`;
const description = intentionData.description;
---

<Layout title={titre} description={description}>
  <!-- Fil d'Ariane -->
  <nav class="container-custom py-4 text-sm text-gray-600">
    <ol class="flex items-center space-x-2">
      <li><a href="/" class="hover:text-primary-600">Accueil</a></li>
      <li class="text-gray-400">›</li>
      <li><a href="/services" class="hover:text-primary-600">Services</a></li>
      <li class="text-gray-400">›</li>
      <li><a href={`/services/${service.id}`} class="hover:text-primary-600">{service.nomCourt}</a></li>
      <li class="text-gray-400">›</li>
      <li><a href={`/services/${service.id}/${metier.id}`} class="hover:text-primary-600">{metier.nomCourt}</a></li>
      <li class="text-gray-400">›</li>
      <li class="text-gray-900 font-medium">{ville.nom}</li>
    </ol>
  </nav>

  <!-- Hero avec intention transactionnelle -->
  <section class="relative bg-gradient-to-r from-primary-600 to-primary-800 text-white overflow-hidden">
    {intentionData.urgence === 'haute' && (
        <div class="absolute top-4 right-4">
          <IntentionAnimation intention="transactionnelle" type="badge">
            ⚡ INTERVENTION RAPIDE
          </IntentionAnimation>
        </div>
    )}

    <div class="container-custom py-20 relative z-10">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="text-5xl md:text-6xl font-bold mb-6">{titre}</h1>

        <!-- Accroche persona -->
        <div class="bg-white/10 backdrop-blur-sm p-6 rounded-xl mb-8">
          <div class="flex items-center justify-center gap-4">
            <div class="w-16 h-16 bg-white/20 rounded-full flex items-center justify-center flex-shrink-0 text-3xl">
              {personaLocal ? '👤' : '👥'}
            </div>
            <div class="text-left">
              <p class="text-lg font-medium mb-1">
                {personaLocal
                  ? `Comme ${persona.nom}, ${metier.nomCourt} à ${ville.nom}...`
                  : `Comme d'autres ${metier.nomCourt} à ${ville.nom}...`
                }
              </p>
              <p class="text-white/90 italic">
                "{personaLocal ? persona.citation : (personaGenerique?.citation || "Je veux être plus visible dans ma ville.")}"
              </p>
            </div>
          </div>
        </div>

        <p class="text-2xl mb-8 text-white/90">{intentionData.description}</p>

        <!-- CTA principal -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <IntentionAnimation intention="transactionnelle" type="button" class="text-lg px-8 py-4">
            <a href="/contact" class="flex items-center gap-2">
              <span>📞</span>
              <span>{intentionData.cta}</span>
            </a>
          </IntentionAnimation>

          <a href={`/etudes-de-cas/${metier.id === 'artisans' ? 'plombier-meru' :
            metier.id === 'commerces' ? 'caviste-cergy' :
              metier.id === 'profession-liberale' ? 'avocat-montmorency' :
                metier.id === 'tpe-pme' ? 'transport-compiegne' :
                  metier.id === 'reseaux-franchise' ? 'franchise-cergy' : 'hotel-chantilly'}`}
             class="bg-white text-primary-600 hover:bg-gray-100 px-8 py-4 rounded-full font-semibold text-lg transition-colors">
            Voir un cas concret
          </a>
        </div>

        <p class="text-sm mt-4 text-white/80">
          Ou appelez-moi directement : <a href="tel:0660921008" class="font-bold underline">06 60 92 10 08</a>
        </p>
      </div>
    </div>
  </section>

  <!-- SECTION 1 : Pourquoi c'est important ici -->
  <section class="py-20">
    <div class="container-custom">
      <div class="grid md:grid-cols-2 gap-12 items-center">
        <div>
          <h2 class="text-3xl font-bold mb-6">
            Pourquoi les {metier.nomCourt} à {ville.nom} ont besoin de {service.nomCourt}
          </h2>
          <p class="text-lg text-gray-700 mb-6">
            {ville.description || `À ${ville.nom} et dans ses environs, la concurrence est réelle.`}
          </p>
          <ul class="space-y-3">
            <li class="flex items-start gap-2">
              <span class="text-primary-600 text-xl">✓</span>
              <span><strong>Concurrence locale</strong> – {ville.alentours.length} communes autour cherchent des {metier.nomCourt}</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-primary-600 text-xl">✓</span>
              <span><strong>Visibilité immédiate</strong> – Apparaissez dans le pack Google local</span>
            </li>
            <li class="flex items-start gap-2">
              <span class="text-primary-600 text-xl">✓</span>
              <span><strong>Clients qualifiés</strong> – Ceux qui cherchent près de chez vous</span>
            </li>
          </ul>
        </div>
        <div class="bg-gray-50 p-8 rounded-xl">
          <h3 class="text-2xl font-bold mb-4">Ce que je vous propose</h3>
          <ul class="space-y-3 mb-6">
            <li class="flex items-start gap-2">📊 Audit gratuit de votre visibilité à {ville.nom}</li>
            <li class="flex items-start gap-2">🎯 Stratégie sur-mesure pour les {metier.nomCourt}</li>
            <li class="flex items-start gap-2">📍 Ciblage des {ville.alentours.length} communes alentours</li>
          </ul>
          <a href="/methodologie" class="text-primary-600 hover:underline">Découvrir ma méthode →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- SECTION 2 : Villes alentours -->
  <section class="bg-gray-50 py-16">
    <div class="container-custom">
      <h2 class="text-3xl font-bold text-center mb-4">J'interviens aussi dans les environs</h2>
      <p class="text-center text-gray-600 mb-8">Je me déplace dans toutes ces communes proches de {ville.nom}.</p>

      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-3 max-w-4xl mx-auto">
        {ville.alentours.map(alentour => {
          const nomAlentour = typeof alentour === 'string' ? alentour.split(' (')[0] : alentour;
          return (
              <div class="bg-white p-3 rounded-lg text-center border border-gray-100 hover:shadow-sm">
                <div class="font-medium">{nomAlentour}</div>
                <div class="text-xs text-gray-500 mt-1">intervention possible</div>
              </div>
          );
        })}
      </div>
    </div>
  </section>

  <!-- SECTION 3 : Autres villes pour ce service+métier -->
  <section class="py-16">
    <div class="container-custom">
      <h3 class="text-xl font-bold text-center mb-6">Également disponible dans d'autres villes</h3>
      <div class="flex flex-wrap justify-center gap-3">
        {autresVilles.map(autreVille => (
            <a
                href={`/services/${service.id}/${metier.id}/${autreVille.id}`}
                class="bg-gray-100 px-4 py-2 rounded-full text-sm hover:bg-primary-100 hover:text-primary-600 transition-colors"
            >
              {autreVille.nom}
            </a>
        ))}
        <a href="/villes" class="bg-primary-50 text-primary-600 px-4 py-2 rounded-full text-sm hover:bg-primary-100 transition-colors">
          Toutes les villes
        </a>
      </div>
    </div>
  </section>

  <!-- SECTION 4 : Autres services pour ce métier dans cette ville -->
  <section class="bg-gray-50 py-20">
    <div class="container-custom">
      <h2 class="text-3xl font-bold text-center mb-4">D'autres services pour les {metier.nomCourt} à {ville.nom}</h2>
      <p class="text-center text-gray-600 mb-12">Complétez votre stratégie avec ces solutions.</p>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 max-w-5xl mx-auto">
        {autresServices.map(autreService => (
            <a
                href={`/services/${autreService.id}/${metier.id}/${ville.id}`}
                class="bg-white p-6 rounded-xl border border-gray-100 hover:shadow-md transition-all group"
            >
              <h3 class="text-lg font-bold mb-2 group-hover:text-primary-600">{autreService.nom}</h3>
              <p class="text-sm text-gray-600 mb-3">{autreService.description}</p>
              <span class="text-primary-600 text-sm group-hover:translate-x-2 transition-transform inline-block">
              Découvrir →
            </span>
            </a>
        ))}
      </div>
    </div>
  </section>

  <!-- SECTION 5 : Témoignages clients (preuve sociale) -->
  <section class="py-20 bg-gray-50">
    <div class="container-custom">
      <h2 class="text-3xl font-bold text-center mb-4">Ils nous ont fait confiance</h2>
      <p class="text-center text-gray-600 mb-12">Découvrez ce que disent d'autres {metier.nomCourt} comme vous.</p>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-6xl mx-auto">
        {temoignagesAAfficher.map((temoignage, index) => (
            <div class="bg-white p-6 rounded-2xl shadow-md hover:shadow-lg transition-all">
              <div class="flex items-start gap-4 mb-4">
                <div class="w-16 h-16 bg-primary-100 rounded-full flex items-center justify-center flex-shrink-0 text-2xl">
                  {temoignage.icone || '👤'}
                </div>
                <div>
                  <p class="font-semibold text-lg">{temoignage.nom}</p>
                  <p class="text-sm text-gray-600">{temoignage.entreprise}</p>
                  <p class="text-xs text-gray-500">{temoignage.ville}</p>
                </div>
              </div>

              <div class="mb-3 text-yellow-400 text-lg">
                ★★★★★
              </div>

              <blockquote class="text-gray-700 italic mb-4">
                "{temoignage.citation}"
              </blockquote>

              <div class="mt-3 text-green-700 font-bold">
                {temoignage.resultat}
              </div>

              <a href={`/etudes-de-cas/${temoignage.etude}`} class="inline-block mt-4 text-primary-600 text-sm hover:underline">
                Voir son histoire →
              </a>
            </div>
        ))}
      </div>

      <div class="text-center mt-12">
        <a href="/etudes-de-cas" class="text-primary-600 hover:underline font-medium">
          Voir toutes les études de cas →
        </a>
      </div>
    </div>
  </section>

  <!-- SECTION 6 : Ressources associées -->
  <section class="bg-gray-50 py-20">
    <div class="container-custom">
      <h2 class="text-3xl font-bold text-center mb-4">Ressources pour les {metier.nomCourt} à {ville.nom}</h2>
      <p class="text-center text-gray-600 mb-12">Guides et articles pour approfondir.</p>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
        <!-- Guide SEO local -->
        <a href="/ressources/guides/seo-local-artisans-2025" class="bg-white p-6 rounded-xl border border-gray-100 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">📘</div>
          <h3 class="font-bold mb-2">Guide SEO local pour {metier.nomCourt}</h3>
          <p class="text-sm text-gray-600 mb-3">Toutes les clés pour être visible dans votre ville.</p>
          <span class="text-primary-600 text-sm">Télécharger →</span>
        </a>

        <!-- Article avis Google -->
        <a href="/ressources/blog/avis-google-importance" class="bg-white p-6 rounded-xl border border-gray-100 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">📝</div>
          <h3 class="font-bold mb-2">Pourquoi les avis sont cruciaux</h3>
          <p class="text-sm text-gray-600 mb-3">Comment obtenir plus d'avis et mieux convertir.</p>
          <span class="text-primary-600 text-sm">Lire l'article →</span>
        </a>

        <!-- Formation -->
        <a href="/services/formation" class="bg-white p-6 rounded-xl border border-gray-100 hover:shadow-md transition-all">
          <div class="text-3xl mb-3">🎓</div>
          <h3 class="font-bold mb-2">Formation SEO pour {metier.nomCourt}</h3>
          <p class="text-sm text-gray-600 mb-3">Devenez autonome sur votre référencement.</p>
          <span class="text-primary-600 text-sm">En savoir plus →</span>
        </a>
      </div>
    </div>
  </section>

  <!-- SECTION 7 : FAQ spécifique -->
  <section class="py-20">
    <div class="container-custom max-w-3xl">
      <h2 class="text-3xl font-bold text-center mb-4">Questions fréquentes</h2>
      <p class="text-center text-gray-600 mb-12">Sur le {service.nom} pour les {metier.nomCourt} à {ville.nom}.</p>

      <div class="space-y-4">
        <details class="bg-gray-50 p-6 rounded-lg">
          <summary class="font-semibold cursor-pointer">Combien de temps pour voir des résultats à {ville.nom} ?</summary>
          <p class="mt-3 text-gray-600">Les premiers résultats apparaissent généralement entre 1 et 3 mois. Pour les {metier.nomCourt} à {ville.nom}, on voit souvent des améliorations rapides sur Google My Business.</p>
        </details>

        <details class="bg-gray-50 p-6 rounded-lg">
          <summary class="font-semibold cursor-pointer">Quel budget pour un {metier.nomCourt} à {ville.nom} ?</summary>
          <p class="mt-3 text-gray-600">Je propose des devis personnalisés après un audit gratuit. Pour {ville.nom}, comptez entre 300€ et 800€ par mois selon vos objectifs.</p>
        </details>

        <details class="bg-gray-50 p-6 rounded-lg">
          <summary class="font-semibold cursor-pointer">Intervenez-vous aussi dans les villages autour ?</summary>
          <p class="mt-3 text-gray-600">Oui ! Je me déplace dans toutes les communes autour de {ville.nom} : {ville.alentours.slice(0, 3).map(a => typeof a === 'string' ? a.split(' (')[0] : a).join(', ')} et bien d'autres.</p>
        </details>
      </div>

      <div class="text-center mt-8">
        <a href="/ressources/faq" class="text-primary-600 hover:underline">
          Voir toutes les FAQ →
        </a>
      </div>
    </div>
  </section>

  <!-- SECTION 8 : Autres métiers dans cette ville -->
  <section class="bg-gray-50 py-16">
    <div class="container-custom">
      <h3 class="text-lg font-semibold text-center mb-6">Ce service à {ville.nom} pour d'autres métiers</h3>
      <div class="flex flex-wrap justify-center gap-3">
        {metiers.filter(m => m.id !== metier.id).slice(0, 5).map(autreMetier => (
            <a
                href={`/services/${service.id}/${autreMetier.id}/${ville.id}`}
                class="bg-white px-4 py-2 rounded-full text-sm hover:bg-primary-100 hover:text-primary-600 transition-colors"
            >
              {autreMetier.nomCourt}
            </a>
        ))}
        <a href="/metiers" class="bg-primary-50 text-primary-600 px-4 py-2 rounded-full text-sm hover:bg-primary-100 transition-colors">
          Tous les métiers
        </a>
      </div>
    </div>
  </section>

  <!-- SECTION 9 : CTA final -->
  <section class="bg-primary-600 text-white py-20">
    <div class="container-custom text-center max-w-3xl">
      <h2 class="text-4xl font-bold mb-4">
        Vous êtes {metier.nomCourt} à {ville.nom} ?
      </h2>
      <p class="text-xl mb-8 opacity-90">
        Je vous propose un audit gratuit pour analyser votre visibilité locale et voir comment attirer plus de clients.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/contact" class="bg-white text-primary-600 hover:bg-gray-100 px-8 py-4 rounded-full font-semibold text-lg transition-colors">
          Demander mon audit gratuit
        </a>
        <a href="tel:0660921008" class="bg-transparent border-2 border-white text-white hover:bg-white/10 px-8 py-4 rounded-full font-semibold text-lg transition-colors">
          📞 06 60 92 10 08
        </a>
      </div>

      <p class="text-sm mt-6 text-white/80">
        Disponible 7j/7 • Intervention rapide dans le {ville.zone === "60" ? "60" : "95"}
      </p>
    </div>
  </section>

  <!-- CTA flottant mobile -->
  <div class="fixed bottom-0 left-0 right-0 bg-white shadow-lg p-4 md:hidden z-50 border-t border-primary-200">
    <div class="flex gap-4">
      <a href="tel:0660921008" class="flex-1 bg-primary-600 text-white py-3 rounded-full font-bold text-center">
        📞 APPELER
      </a>
      <a href="/contact" class="flex-1 bg-primary-600 text-white py-3 rounded-full font-bold text-center">
        AUDIT GRATUIT
      </a>
    </div>
  </div>
</Layout>
---
// src/components/ServicesSplit.astro

import services from "../data/services.json";
---

<div class="section bg-transparent mt-4 mb-0 pb-0">
  <div class="container">
    <div class="heading-block border-bottom-0 text-center mx-auto mb-0" style="max-width: 650px">
      <div class="badge rounded-pill badge-default">Services</div>
      <h2 class="text-transform-none ls-0 mb-3">Tout ce qu’il faut pour générer des demandes</h2>
      <p>On fait simple. On priorise. On mesure.</p>
    </div>

    <div class="row g-4 mt-4">
      {services.map((s) => (
        <div class="col-md-6 col-lg-4">
          <a href={`/services/${s.slug}/`} class="text-decoration-none">
            <div class="feature-box border-0 p-4 rounded-6 bg-white shadow-sm h-100">
              <div class="fbox-content">
                <h3 class="text-transform-none ls-0 mb-2">{s.title}</h3>
                <p class="mb-0 text-contrast-600">{s.benefit}</p>
              </div>
            </div>
          </a>
        </div>
      ))}
    </div>
  </div>
</div>
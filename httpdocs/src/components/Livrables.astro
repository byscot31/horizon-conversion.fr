---
// src/components/Livrables.astro
import deliverables from "../data/deliverables.json";
import { resolveByIds } from "../lib/resolve";

const { ids, title = "Ce qu’on met en place" } = Astro.props;
const items = resolveByIds(deliverables, ids || []);
---

<section class="container py-4">
  <div class="d-flex align-items-end justify-content-between gap-3 flex-wrap">
    <h2 class="h3 fw-bold mb-0">{title}</h2>
    <p class="text-muted mb-0">Du concret. Pas de blabla.</p>
  </div>

  <ul class="mt-3 mb-0">
    {items.map((it) => (
      <li class="mb-2">{it.text}</li>
    ))}
  </ul>

  <div class="mt-4 p-3 border rounded-3 bg-light">
    <div class="d-flex flex-wrap align-items-center justify-content-between gap-2">
      <p class="mb-0"><strong>Envie d’un plan clair ?</strong> On le pose pendant l’audit gratuit.</p>
      <a class="btn btn-primary" href="/audit">Demander un audit gratuit</a>
    </div>
  </div>
</section>
